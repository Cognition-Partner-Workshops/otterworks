-- The feedback bounded context's schema, moved here from
-- services/legacy-portal/scripts/initdb.sql when feedback was extracted out of the
-- monolith. This service owns the schema exclusively.
CREATE SCHEMA IF NOT EXISTS feedback;
