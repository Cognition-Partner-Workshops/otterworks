<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <includeMdcKeyName>traceId</includeMdcKeyName>
      <includeMdcKeyName>spanId</includeMdcKeyName>
      <fieldNames>
        <timestamp>timestamp</timestamp>
        <message>message</message>
        <logger>logger</logger>
        <thread>thread</thread>
        <level>level</level>
      </fieldNames>
      <customFields>{"service":"analytics-service"}</customFields>
    </encoder>
  </appender>

  <logger name="com.otterworks.analytics" level="DEBUG"/>
  <logger name="akka" level="INFO"/>
  <logger name="slick" level="INFO"/>
  <!-- Iceberg/Hadoop/Parquet are verbose at INFO; keep the lakehouse backend quiet. -->
  <logger name="org.apache.hadoop" level="WARN"/>
  <logger name="org.apache.iceberg" level="WARN"/>
  <logger name="org.apache.parquet" level="WARN"/>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
