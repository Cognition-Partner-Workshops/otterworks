# Local dev configuration read by docker compose and the Makefile.
# Copy to .env (or run `make .env` to generate one with a random password).
# Shared PostgreSQL password for the local stack (postgres container + every service).
POSTGRES_PASSWORD=
