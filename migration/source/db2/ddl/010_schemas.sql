-- Db2 11.5 LUW. Run with: db2 -tvf <file> (statement terminator ';').
-- Database: one per demo namespace (CONTRACTS.md §5.2), created UTF-8 (CODESET UTF-8 TERRITORY US).

CREATE SCHEMA ARCHIVE;
CREATE SCHEMA MIGAUDIT;
