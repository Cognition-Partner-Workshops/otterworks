# Recorded before-state for the incident-responder gate. Do not edit by hand;
# re-pin with `make incident-record REASON="..."` -- the reason is kept here
# so every re-record is auditable in git history.
recorded:
  at: '2026-09-24T15:32:55.876882+00:00'
  git_sha: 905ea0cbc53c2e568b78b4618347ca165d248497
  reason: 're-pinned after PR #1669 review remediations: cached/bounded chaos flag
    reads (app/chaos.py), credential-header redaction in the request log (headers
    only; response-body retention and unbounded growth stay planted), delayed duplicate-window
    gauge refresh (stats_rollup.py), generalized armed after-gate with per-scenario
    after thresholds (incident.py, scenarios.yaml), replica scrape target (prometheus.yml),
    loopback-only alert sink. No seed, load-profile or alert-rule threshold change.'
fingerprints:
  fixture: 9f3aa87f18d5152274f7e98743739c69da3b31ec2312933c63bf0636bdfb40c6
  source: b7fe1a41c334322f23abbbabff0649fbe8408092370221c0afc27baa9683d829
scenarios:
  n-plus-one:
    alert: DocumentListLatencyHigh
    fires_within_seconds: 180
  log-flood:
    alert: RequestLogVolumeNearFull
    fires_within_seconds: 240
  cache-leak:
    alert: DocumentServiceMemoryHigh
    fires_within_seconds: 420
  double-run:
    alert: DocumentStatsRollupDuplicated
    fires_within_seconds: 240
