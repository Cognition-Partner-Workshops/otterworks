# Recorded before-state for the incident-responder gate. Do not edit by hand;
# re-pin with `make incident-record REASON="..."` -- the reason is kept here
# so every re-record is auditable in git history.
recorded:
  at: '2026-09-24T19:29:47.724832+00:00'
  git_sha: f997af578825c55427a66de77c0bf967c2b52cc6
  reason: 'INCIDENT_LOAD_SCALE: scale a scenario''s rps/concurrency for targets with
    less headroom than the local stack (the single 500m-CPU tenant replica on the
    shared cluster saturates under the laptop profile even after the fix). Default
    1 -- the local gates, load profiles, alert thresholds and seed are unchanged.'
fingerprints:
  fixture: 16aa8b6d057f613f026584447fd792b8eb5741ff79718580490193dd354cece9
  source: 480b6fe74ff60a0d4d339ae8c3d8af03d23f45cd6e565c438481596bbcaa553a
scenarios:
  n-plus-one:
    alert: DocumentListLatencyHigh
    fires_within_seconds: 180
  log-flood:
    alert: RequestLogVolumeNearFull
    fires_within_seconds: 240
  cache-leak:
    alert: DocumentServiceMemoryHigh
    fires_within_seconds: 420
  double-run:
    alert: DocumentStatsRollupDuplicated
    fires_within_seconds: 240
