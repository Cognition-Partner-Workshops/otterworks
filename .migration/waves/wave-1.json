{
 "wave": 1,
 "repo": "github.com/Cognition-Partner-Workshops/otterworks",
 "base_branch": "tp-run/mongodb-20260926T164927Z-rt-live",
 "child_macro": "!mongo_unit_migration",
 "verify_macro": "!mongo_reconciliation",
 "width": 3,
 "breaker_threshold": 3,
 "auto_merge": true,
 "source_access": "live",
 "target_access": "migration_cluster",
 "child_minutes": 45,
 "execution": "single-session (playbook 2 step 5: <10 units, ~204k rows; AND the source is reachable only from this VM, so fan-out children on other machines cannot read it). Batches run in this session via !mongo_unit_migration; verification via !mongo_reconciliation as recorded at STOP B.",
 "expected_cost_note": "wave 1 is the calibration wave for two pattern classes: wide-embed (customers, XL 155 cols) and proc-heavy (subscriptions_rating/invoicing/dunning). No later wave to discount against.",
 "batches": [
  {
   "id": "w1-b01",
   "units": [
    "customers"
   ],
   "write_targets": [
    "mmp_rt_billing.customers"
   ],
   "fixture_manifest": ".migration/fixtures/ow_billing.json",
   "size_class": "XL",
   "brief": "Units: customers (CUSTOMER_MASTER + embedded ENTITY_ATTR_VALUE). Source: Oracle OW_BILLING via secret ORACLE_BILLING_RO_DSN (read-only, concurrency 1). Target: Atlas otterworks-demo database mmp_rt_billing via secret MONGODB_MMP_RT_TARGET_URI; write ONLY to mmp_rt_billing.customers. Mapping: .migration/03_mapping_spec.json v1.0.0 (fields, keys, embeds, canonicalization). Tolerances: .migration/02_tolerances.json v1.0.0 (exact). Fixture manifest: .migration/fixtures/ow_billing.json. Loader: Python oracledb -> pymongo bulk_write (replace, upsert) into the declared collections, idempotent by _id. Recon: `recon run` from repo root, --mode fixture until green, then exactly one --mode live --target-class migration_cluster; merge only on live PASS. Never touch Oracle beyond SELECT; never edit .migration/; open one PR per batch against the run branch. XL unit: write the short decision-first contract PR section before code (playbook 3)."
  },
  {
   "id": "w1-b02",
   "units": [
    "invoice_batch"
   ],
   "write_targets": [
    "mmp_rt_billing.invoice_headers",
    "mmp_rt_billing.invoice_line_orphans"
   ],
   "fixture_manifest": ".migration/fixtures/ow_billing.json",
   "brief": "Units: invoice_batch (INVOICE_HEADER + embedded INVOICE_LINE; 37 orphans -> invoice_line_orphans). Source: Oracle OW_BILLING via secret ORACLE_BILLING_RO_DSN (read-only, concurrency 1). Target: Atlas otterworks-demo database mmp_rt_billing via secret MONGODB_MMP_RT_TARGET_URI; write ONLY to mmp_rt_billing.invoice_headers, mmp_rt_billing.invoice_line_orphans. Mapping: .migration/03_mapping_spec.json v1.0.0 (fields, keys, embeds, canonicalization). Tolerances: .migration/02_tolerances.json v1.0.0 (exact). Fixture manifest: .migration/fixtures/ow_billing.json. Loader: Python oracledb -> pymongo bulk_write (replace, upsert) into the declared collections, idempotent by _id. Recon: `recon run` from repo root, --mode fixture until green, then exactly one --mode live --target-class migration_cluster; merge only on live PASS. Never touch Oracle beyond SELECT; never edit .migration/; open one PR per batch against the run branch."
  },
  {
   "id": "w1-b03",
   "units": [
    "subscriptions_rating",
    "invoicing",
    "dunning"
   ],
   "write_targets": [
    "mmp_rt_billing.subscriptions",
    "mmp_rt_billing.usage_events",
    "mmp_rt_billing.rating_periods",
    "mmp_rt_billing.invoices",
    "mmp_rt_billing.credit_notes",
    "mmp_rt_billing.dunning_attempts",
    "mmp_rt_billing.notifications"
   ],
   "fixture_manifest": ".migration/fixtures/ow_billing.json",
   "brief": "Units: subscriptions_rating (pkg_plans, pkg_rating), invoicing (pkg_invoicing), dunning (pkg_dunning). Source: Oracle OW_BILLING via secret ORACLE_BILLING_RO_DSN (read-only, concurrency 1). Target: Atlas otterworks-demo database mmp_rt_billing via secret MONGODB_MMP_RT_TARGET_URI; write ONLY to mmp_rt_billing.subscriptions, mmp_rt_billing.usage_events, mmp_rt_billing.rating_periods, mmp_rt_billing.invoices, mmp_rt_billing.credit_notes, mmp_rt_billing.dunning_attempts, mmp_rt_billing.notifications. Mapping: .migration/03_mapping_spec.json v1.0.0 (fields, keys, embeds, canonicalization). Tolerances: .migration/02_tolerances.json v1.0.0 (exact). Fixture manifest: .migration/fixtures/ow_billing.json. Loader: Python oracledb -> pymongo bulk_write (replace, upsert) into the declared collections, idempotent by _id. Recon: `recon run` from repo root, --mode fixture until green, then exactly one --mode live --target-class migration_cluster; merge only on live PASS. Never touch Oracle beyond SELECT; never edit .migration/; open one PR per batch against the run branch. Port the four PL/SQL packages to app-side Python (services/legacy-billing/app/backends/mongo.py) with request-scoped state replacing package globals."
  }
 ]
}