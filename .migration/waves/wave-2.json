{
 "wave": 2,
 "repo": "github.com/Cognition-Partner-Workshops/otterworks",
 "branch": "tp-run/mongodb-20260922T142645Z",
 "backend": "local-sequential",
 "child_macro": "playbook file skills/install-mongo-kit/playbooks/3-unit_migration.md executed by hand (no !mongo_* macros in this install)",
 "verify_macro": "skills/install-mongo-kit/playbooks/4-reconciliation_and_parallel_run.md, fixture mode only",
 "width": 1,
 "breaker_threshold": 3,
 "auto_merge": false,
 "child_minutes": 45,
 "recon_mode": "offline",
 "allowed_targets": ".migration/allowed_targets.json",
 "depends_on_waves": [
  0,
  1
 ],
 "expected_cost_note": "3 proc-heavy units; expect 30-50% below wave 1 per unit as the pattern is reused",
 "batches": [
  {
   "id": "w2-b01",
   "units": [
    "usage-rating"
   ],
   "source_tables": [
    "USAGE_EVENTS",
    "RATING_PERIODS",
    "RATING_RESULTS"
   ],
   "collections": [
    "usageEvents",
    "ratingPeriods"
   ],
   "write_targets": [
    "ow_billing_offline.usageEvents",
    "ow_billing_offline.ratingPeriods"
   ],
   "fixture_manifest": ".migration/fixtures/w2-b01.json",
   "brief": "Usage and rating (pkg_rating). ratingPeriods embeds RATING_RESULTS as results[] (1:few, derived, 03_pkg_rating.sql:88,199-209); harness counts sum(results[].length) = count(RATING_RESULTS). usageEvents one document per event, index (tenantId, occurredAt); trg_usage_events_check (01_tables.sql:238-252) becomes JSON schema validation units>0 + app check. Seed: periods with 0, 1 and several results; events at TIMESTAMP ms precision; kind_cd unknown to CODES.\n\nRepo otterworks, branch tp-run/mongodb-20260922T142645Z (PR target; never tech-partnerships/main). recon_mode offline, stop_mode hard: run every command as `env -u MONGODB_ATLAS_URI ...`; local Docker only (Oracle Free 127.0.0.1:1521 FREEPDB1 fixture built from services/legacy-billing/db/oracle/{schema,packages}/*.sql minus 04_upgrade_static.sql (D-010), MongoDB mongo:7 127.0.0.1:27017, target db ow_billing_offline only). Legacy tree services/legacy-billing/ is read-only. Contract: .migration/02_tolerances.json (tol-1), mapping .migration/03_mapping_spec.json (map-draft-2, regenerate only via model_patch.py + 05_decisions.json), census .migration/census.json. Fixture: synthetic seed under migration/mongo/fixtures/ that exercises every census trap for this unit's tables (string dates incl. unparseable, Y/N flags incl. NULL, CSV lists incl. malformed, repeating groups, orphans/unenforced pointers, sequence ids, empty strings, CHAR padding); write .migration/fixtures/<batch>.json {source,method,masked_columns,produced_at,produced_by,row_counts}. Loader: migration/mongo/loaders/<unit>.py, spec-driven from 03_mapping_spec.json, idempotent (upsert by key; rerun yields zero diff). Verify: harness `recon run --mode fixture` (venv /home/ubuntu/mmp-venv) PASS, then one fault injection (delete/alter one target doc or field) proving FAIL, then rerun loader and PASS again; evidence redacted under .migration/recon/<unit>/ incl. recon.summary.md. `make tp-smoke` before the PR. One held PR per unit into the tp-run branch: sections Decisions / Code / Evidence, unverified paths first, recon.summary.md rendered, `merge_eligible: false` stated. Do not merge. Cap 3 recon reruns; same-class failure x3 across units halts the wave."
  },
  {
   "id": "w2-b02",
   "units": [
    "invoicing"
   ],
   "source_tables": [
    "INVOICES",
    "INVOICE_LINES",
    "CREDIT_NOTES"
   ],
   "collections": [
    "invoices",
    "creditNotes"
   ],
   "write_targets": [
    "ow_billing_offline.invoices",
    "ow_billing_offline.creditNotes"
   ],
   "fixture_manifest": ".migration/fixtures/w2-b02.json",
   "brief": "Invoicing (pkg_invoicing). invoices embeds INVOICE_LINES as lines[] keyed lineNo (1:few, derived: 04_pkg_invoicing.sql:150-157 rebuilds all lines with the invoice; FK ON DELETE CASCADE 01_tables.sql:132); harness counts sum(lines[].length) = count(INVOICE_LINES). creditNotes separate (burned oldest-first by sp_issue_invoice; read independently). Seed: invoices with 0..6 lines, NUMBER(12,2) amounts at scale edges (x.xx5), status codes, credits partially burned.\n\nRepo otterworks, branch tp-run/mongodb-20260922T142645Z (PR target; never tech-partnerships/main). recon_mode offline, stop_mode hard: run every command as `env -u MONGODB_ATLAS_URI ...`; local Docker only (Oracle Free 127.0.0.1:1521 FREEPDB1 fixture built from services/legacy-billing/db/oracle/{schema,packages}/*.sql minus 04_upgrade_static.sql (D-010), MongoDB mongo:7 127.0.0.1:27017, target db ow_billing_offline only). Legacy tree services/legacy-billing/ is read-only. Contract: .migration/02_tolerances.json (tol-1), mapping .migration/03_mapping_spec.json (map-draft-2, regenerate only via model_patch.py + 05_decisions.json), census .migration/census.json. Fixture: synthetic seed under migration/mongo/fixtures/ that exercises every census trap for this unit's tables (string dates incl. unparseable, Y/N flags incl. NULL, CSV lists incl. malformed, repeating groups, orphans/unenforced pointers, sequence ids, empty strings, CHAR padding); write .migration/fixtures/<batch>.json {source,method,masked_columns,produced_at,produced_by,row_counts}. Loader: migration/mongo/loaders/<unit>.py, spec-driven from 03_mapping_spec.json, idempotent (upsert by key; rerun yields zero diff). Verify: harness `recon run --mode fixture` (venv /home/ubuntu/mmp-venv) PASS, then one fault injection (delete/alter one target doc or field) proving FAIL, then rerun loader and PASS again; evidence redacted under .migration/recon/<unit>/ incl. recon.summary.md. `make tp-smoke` before the PR. One held PR per unit into the tp-run branch: sections Decisions / Code / Evidence, unverified paths first, recon.summary.md rendered, `merge_eligible: false` stated. Do not merge. Cap 3 recon reruns; same-class failure x3 across units halts the wave."
  },
  {
   "id": "w2-b03",
   "units": [
    "dunning-ops"
   ],
   "source_tables": [
    "DUNNING_ATTEMPTS",
    "NOTIFICATIONS",
    "BILLING_AUDIT_LOG"
   ],
   "collections": [
    "dunningAttempts",
    "notifications",
    "billingAuditLog"
   ],
   "write_targets": [
    "ow_billing_offline.dunningAttempts",
    "ow_billing_offline.notifications",
    "ow_billing_offline.billingAuditLog"
   ],
   "fixture_manifest": ".migration/fixtures/w2-b03.json",
   "brief": "Dunning and operational logs (pkg_dunning, pkg_ow_util.log_msg). billingAuditLog keeps numeric logId as _id and gets a TTL index on loggedAt expireAfterSeconds=7776000 (replaces JOB_PURGE_AUDIT_LOG, 04_jobs.sql:19-27); the fixture recon runs before any TTL expiry (seed loggedAt within 90 days) so counts match. JOB_NIGHTLY_DUNNING becomes a billing-service scheduled job (owner: STOP B U-5). Seed: idempotent notifications (dedupe key), attempts across statuses, audit rows with 4000-char messages and NULL module.\n\nRepo otterworks, branch tp-run/mongodb-20260922T142645Z (PR target; never tech-partnerships/main). recon_mode offline, stop_mode hard: run every command as `env -u MONGODB_ATLAS_URI ...`; local Docker only (Oracle Free 127.0.0.1:1521 FREEPDB1 fixture built from services/legacy-billing/db/oracle/{schema,packages}/*.sql minus 04_upgrade_static.sql (D-010), MongoDB mongo:7 127.0.0.1:27017, target db ow_billing_offline only). Legacy tree services/legacy-billing/ is read-only. Contract: .migration/02_tolerances.json (tol-1), mapping .migration/03_mapping_spec.json (map-draft-2, regenerate only via model_patch.py + 05_decisions.json), census .migration/census.json. Fixture: synthetic seed under migration/mongo/fixtures/ that exercises every census trap for this unit's tables (string dates incl. unparseable, Y/N flags incl. NULL, CSV lists incl. malformed, repeating groups, orphans/unenforced pointers, sequence ids, empty strings, CHAR padding); write .migration/fixtures/<batch>.json {source,method,masked_columns,produced_at,produced_by,row_counts}. Loader: migration/mongo/loaders/<unit>.py, spec-driven from 03_mapping_spec.json, idempotent (upsert by key; rerun yields zero diff). Verify: harness `recon run --mode fixture` (venv /home/ubuntu/mmp-venv) PASS, then one fault injection (delete/alter one target doc or field) proving FAIL, then rerun loader and PASS again; evidence redacted under .migration/recon/<unit>/ incl. recon.summary.md. `make tp-smoke` before the PR. One held PR per unit into the tp-run branch: sections Decisions / Code / Evidence, unverified paths first, recon.summary.md rendered, `merge_eligible: false` stated. Do not merge. Cap 3 recon reruns; same-class failure x3 across units halts the wave."
  }
 ]
}