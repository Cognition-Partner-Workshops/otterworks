# Dependency register

Classes: D1 intra-pipeline lineage · D2 shared object · D3 upstream feed · D4 downstream consumer ·
D5 scheduler · D6 shared-write table · D7 external hand-off · D8 security/governance · D9 ML consumer ·
D10 environment/access. Appended by later playbooks; never rewritten.

| ID | Class | Contract (what crosses, who owns it) | Decision | Routing point | Cutover / decommission condition | Fired request | Status | Blast radius |
|---|---|---|---|---|---|---|---|---|
| D10-1 | D10 | Oracle supplemental logging (`ALTER DATABASE ADD SUPPLEMENTAL LOG DATA` + per-table `(ALL) COLUMNS`) and `c##dbzuser` LogMiner grants. Owner: customer DBA. Statements: attachment `ow_tp_oracle_cdc_prereqs.sql` (never applied by Devin). Live check 2026-09-14: `SUPPLEMENTAL_LOG_DATA_MIN=NO`, no `C##DBZUSER` in `DBA_USERS`. | CDC coexistence gated on this; rehearsal fallback = SCN-pinned freeze-and-load into the Lakebase branch (STOP A row) | Debezium connector start | CDC running with lag < `cdc_lag_max_s` before any operational unit's transactional recon counts as LIVE | fired via parent 2026-09-14 (statements attached to intake) | CLOSED 2026-09-14 (parent, as customer delegate: supplemental logging MIN + ALL COLUMNS on the 10 operational tables, `c##dbzuser` granted; Debezium secret `ow-tp/oracle/dbzuser`) | every operational unit's recon mode |
| D10-2 | D10 | Network path from Databricks serverless (SQL warehouse / Lakeflow) to `52.201.36.9:1521`. SG `sg-0eaf11f4434260e1` admits Devin CIDRs only. Owner: parent applies the SG change once the child reports the authoritative serverless egress IPs for the workspace region. | Report CIDRs (see `07_access_checklist.md` §D10-2 evidence); until applied, federation/JDBC from Databricks is BLOCKED and live recon runs from the Devin VM path (works now) | SG ingress rule | needed only for Databricks-side federation/JDBC; not a blocker for wave 0/1 | this session (STOP A packet) | CLOSED 2026-09-14 (SG admits 1521 from us-west-2 serverless CIDRs) | Lakehouse Federation / JDBC pull from Databricks; not recon |
| D10-3 | D10 | Debezium + Kafka hosting. Proposed: Docker containers on the existing EC2 host `i-0be201ad6412c5e5e` (t3.medium, EC2 exception approved 2026-09-14). Different footprint = new decision; Devin creates no AWS resources without it. | keep proposed footprint (STOP A row) | docker-compose on the host | dropped at decommission | none needed if footprint accepted | ACCEPTED at STOP A (containers on `i-0be201ad6412c5e5`, deploy via SSM) | operational CDC path |
| D10-4 | D10 | Databricks identity is a human PAT (`DATABRICKS_DEMO_TOKEN`, workspace admin), not a migration service principal with OAuth M2M. `factory-doctor` reports `databricks_identity: warn (human)` and `databricks_auth_kind: warn`, so `ready: false` and the fan-out workflow refuses manifests. | STOP A row: (a) accept the PAT identity for this demo run and record it as the engagement identity (doctor `ready` stays false; waves launched by hand at width 4, never via the workflow's identity gate), or (b) parent provisions a migration SP (`DATABRICKS_CLIENT_ID/SECRET`, rights on `ow_tp` only) | `00_context.md` identity row | n/a | this session (STOP A packet) | CLOSED 2026-09-14 (DEC-C: migration SP `dhrov_spa` / applicationId `2e90bc1d-e9a1-4703-8c48-ad28ebb1864d` authenticated via OAuth M2M; doctor identity and auth checks are ok) | every wave launch |
| D10-5 | D10 | Oracle source adapter for `dbx-recon` is untested / no `oracle` extra (`pyproject.toml` extras: databricks, sqlserver, lakebase). Live LIVE-mode recon against Oracle needs either the harness's generic adapter hardened for Oracle (wave 0 item, `python-oracledb` thin) or an SCN-pinned Delta landing of the source as the "source" side. | wave-0 item: add `oracle` extra + adapter tests to the harness, promote to the shared skill | harness | n/a | none (Devin-owned) | OPEN | every recon run |
| D10-6 | D10 | Oracle read-only credential exposure: during this session's first probe a wrapper script echoed the fetched `ow-tp/oracle/ow_billing_ro` DSN (incl. password) once into a tool transcript before being fixed. No artifact or file contains it. Owner: credential owner (parent/customer) should rotate the `ow_billing_ro` password and update the Secrets Manager entry. | rotate | Secrets Manager | n/a | this session (STOP A packet) | CLOSED 2026-09-14 (rotated; re-fetch before every read) | none functional; hygiene |
| D10-7 | D10 | Plugin enforcement hook (`hooks/dbx_guard.py`, PreToolUse) is not applied by this Devin platform: the doctor's nonce probe echoed unblocked (`hook_platform_loaded: fail`) although the guard blocks when invoked directly. The write-scope allowlist is therefore enforced by convention (allowlist + doctor + independent recon), not by a hard block. Owner: org admin / Cognition (plugin hooks at org level). | STOP A row: (a) accept convention-only write-scope enforcement for this demo run (children still run doctor and pre-commit hooks; recon verifies write targets), or (b) hold fan-out until hooks load | doctor `hook_platform_loaded` | n/a | this session (STOP A packet) | ACCEPTED at STOP A (convention-only enforcement for this run) | every child's write safety |
| D5-1 | D5 | `JOB_NIGHTLY_DUNNING` (02:00) and `JOB_PURGE_AUDIT_LOG` (03:30), `DBMS_SCHEDULER`, both DISABLED at source. | to be decided at STOP C (Lakeflow Job `ow_tp_dunning_nightly`, PAUSED, vs pg_cron in Lakebase) | job schedule | enable on target only after STOP E | none | REGISTERED | dunning unit |
| D4-1 | D4 | Application consumers of `OW_BILLING` (connection census not yet done; the OtterWorks billing service and the CUSTBILL/analytics readers are the known candidates). | inventory playbook sweeps config repos for connection strings | app repoint PR (STOP E) | repoint executed by the customer with the cutover principal | none | REGISTERED | STOP E |
| D2-1 | D2 | `CODES`, `PLANS`, `TENANTS`, `CUSTOMER_MASTER` are read by every package and by pipelines 2/3. | migrate once in wave 0 (this pipeline owns them) | wave 0 | n/a | none | REGISTERED | pipelines 2/3 |
| D10-8 | D10 | `OW_BILLING_RO` lacks `FLASHBACK ANY TABLE`: `SELECT ... AS OF SCN` fails (ORA-41900), so cross-session SCN-pinned reads for the analytical freeze-and-load and for recon source pins are impossible from this principal. Owner: customer DBA / parent (one grant, no data change). | UNDECIDED: (a) grant `FLASHBACK ANY TABLE` to `OW_BILLING_RO`, or (b) accept per-session `SET TRANSACTION READ ONLY` snapshots with `CURRENT_SCN` recorded at open/close (recon record `isolation: read_only_txn`) | recon source pin | n/a | inventory 2026-09-14 | CLOSED 2026-09-14 (FLASHBACK ANY TABLE granted; pins = AS OF SCN) | analytical units' load + every recon source pin |
| D4-2 | D4 | Billing report endpoint `services/legacy-billing/app/reports.py` (Flask, admin-dashboard billing-report page) reads `INVOICE_HEADER`, `INVOICE_LINE`, `CODES`, `CUSTOMER_MASTER` via `oracledb` and `ORACLE_*` env. Owner: OtterWorks legacy-billing team. | UNDECIDED: repoint to Delta (DBSQL) or to Lakebase synced `CODES`/`CUSTOMER_MASTER`; repoint PR prepared, customer executes at STOP E | `ORACLE_*` env of the legacy-billing deployment | consumer reads target for one full cycle with recon PASS | inventory 2026-09-14 | UNDECIDED | P3 cutover |
| D4-3 | D4 | Parity harness `procs/harness/oracle_record.py` + `procs/oracle/oracle_map.yaml` calls all 5 packages and resets operational tables (test-only, `DB_*` env). | UNDECIDED: retarget to Lakebase branch for PL/pgSQL parity (it is the natural equivalence oracle for P1 procedures) | `DB_*` env | decommission with the Oracle fixture | inventory 2026-09-14 | UNDECIDED | P1/P2 verification |
| D4-4 | D4 | `scripts/tp_pain/mongodb.py` read-only inspection of `CUSTOMER_MASTER`, `CUSTOMER_MASTER_HIST`, `ENTITY_ATTR_VALUE`; `testdata/legacy/oracle_billing_seed.py` seeder (writes, non-production). | UNDECIDED: no repoint needed (tooling); confirm at STOP C | n/a | none | inventory 2026-09-14 | UNDECIDED | none |
| D4-5 | D4 | No production application in the repo holds an Oracle connection pool that calls `PKG_PLANS`/`PKG_RATING`/`PKG_INVOICING`; the only callers found are the parity harness and the DISABLED scheduler job. The billing application transaction path is INFERRED. Owner: customer (application inventory outside this repo). | UNDECIDED: customer confirms the external callers or confirms none exist | application connection strings | n/a | inventory 2026-09-14 (STOP B question) | ANSWERED at STOP B (customer-stated intended model, DEC-B1); STOP E hard precondition: billing service + usage producers named and usage event -> rating -> invoice -> notification demonstrated | STOP E repoint scope |
| D6-1 | D6 | `TENANTS` is written by P2 (`PKG_DUNNING` UPDATE) and is the FK root for P1/P3 tables. | UNDECIDED: wave-0 shared table in Lakebase; P2 writes land after P1 units | Lakebase branch merge order | n/a | inventory | UNDECIDED | P1, P2 |
| D6-2 | D6 | `SUBSCRIPTIONS` has three writers: `PKG_PLANS` (P1), `PKG_DUNNING` (P2 UPDATE), triggers `TRG_SUBSCRIPTIONS_HIST`/`TRG_SUB_NO_UNCANCEL`. | UNDECIDED: owned by P1; P2 unit depends on P1's SUBSCRIPTIONS unit; triggers convert with the table | unit boundary | n/a | inventory | UNDECIDED | P1, P2 |
| D3-1 | D3 | `USAGE_EVENTS` (814 rows) is the rating feed; no producer found in the repo (seeder only). Owner: customer. | UNDECIDED: producer identity and whether it will write to Lakebase after cutover | ingestion of usage events | n/a | inventory (STOP B question) | ANSWERED at STOP B (DEC-B1: product services -> usage-ingestion service writes USAGE_EVENTS; concrete producers still to be named before STOP E) | P1 rating |
| D7-1 | D7 | `NOTIFICATIONS` rows are a hand-off to a notification sender (none found in repo; `services/notification-service` is Postgres-based, INFERRED not connected). | UNDECIDED | notification consumer | n/a | inventory | PARTIAL (DEC-B1: notification is the last hop of the demonstrated path; sender to be named before STOP E) | P2 |
| D9-1 | D9 | Sequence/identity contracts: `SEQ_CUSTOMER_MASTER` last_number 125,000 vs 25,000 rows; `SEQ_ENTITY_ATTR_VALUE` cache 1000 (gaps); `SEQ_BILLING_AUDIT_LOG`/`SEQ_SUBSCRIPTIONS_HIST`/`SEQ_CUSTOMER_MASTER_HIST` unused so far. Trigger-assigned IDs on 3 tables. | UNDECIDED: Lakebase sequences start at Oracle `LAST_NUMBER`+cache, identity columns replace `BEFORE INSERT` sequence triggers; tolerance row `sequence_parity` | dictionary rows | n/a | inventory | UNDECIDED | P1, P3 |
| D2-2 | D2 | `CODES` is read by P1 (trigger lookup, `PKG_OW_UTIL.F_CODE_DESC`) in Lakebase and by the P3 report in Delta. | UNDECIDED: Lakebase master, synced table to `ow_tp` via Lakehouse Sync | sync definition | n/a | inventory | UNDECIDED | P1, P3 |
| D4-6 | D4 | Monthly scheduler (unnamed, DEC-B1) needs a period-close entry point; Oracle has none (harness issued per tenant). | PROPOSED: new surface `invoicing.close_billing_period(period_start, period_end)` in U4, fixture-tested only, excluded from like-for-like recon | scheduler -> Lakebase | STOP E (customer names scheduler) | P1 plan (STOP C) | UNDECIDED -> STOP C | U4 |
| D7-2 | D7 | Demonstrated path ends in a notification, but Oracle P1 emits none at invoice issue (only `PKG_DUNNING` writes `NOTIFICATIONS`). | PROPOSED option B: keep `sp_issue_invoice` like-for-like; P2 emits invoice notifications. Option A: emit `kind_cd=1` in U4 (new behaviour, recon-excluded) | U4 / P2 boundary | STOP E demo path | P1 plan (STOP C) | UNDECIDED -> STOP C | U4, P2 |
| D9-2 | D9 | Package session state (`pkg_rating.g_*` read by `pkg_invoicing`; `pkg_plans.g_last_*`, `pkg_ow_util.g_*`) is the cross-routine API in Oracle; no external reader found (INFERRED). | PROPOSED: composite return types `rating.rating_result_t`, `invoicing.preview_t`; `g_last_*` caches dropped | procedure interfaces | n/a | P1 analysis | UNDECIDED -> STOP C | U0, U1, U3, U4 |
| D9-3 | D9 | `PKG_OW_UTIL.LOG_MSG` is `PRAGMA AUTONOMOUS_TRANSACTION`; Postgres has no equivalent, so audit rows written in a rolled-back business transaction are lost. | PROPOSED: in-transaction `util.log_msg`; `BILLING_AUDIT_LOG` compared on `(module, message)` multiset with rollback-loss accepted (tolerance row 14) | audit log | n/a | P1 analysis | UNDECIDED -> STOP C | U0 (all pipelines) |
| D10-9 | D10 | Lakehouse Sync (Lakebase -> Delta, needed for analytical copies `SUBSCRIPTIONS_HIST`, `CODES`, `PLANS`, `TENANTS` in `ow_tp.silver`) is UI-only: no CLI/API (databricks-lakebase skill, `references/lakehouse-sync.md`). Owner: parent/customer with workspace UI, per branch schema. | PROPOSED: request enablement on the wave-close branch schema `ow_billing`; freeze-and-load (`AS OF SCN`) for analytical copies until then | Delta copies | n/a | fired at STOP C | OPEN | U0, U1, P3 report |
| D10-10 | D10 | Debezium + Kafka + Connect containers (DEC-A primary CDC) not yet deployed; Kafka -> Delta landing -> Lakebase apply path not built. Owner: this orchestrator via parent (SSM), wave-0 scaffolding. | PROPOSED: wave-0 workstream; until live, transactional recon records Tier 6 `skipped/no_cdc` and pins `AS OF SCN` (fallback approved DEC-A) | CDC apply | n/a | fired at STOP C | OPEN | U1-U4 recon posture |
| D10-11 | D10 | `migration-fanout/workflow.py` refuses manifests unless `09_capabilities.json` is `ready:true`; D10-4 (accepted human PAT, `databricks_identity=warn`) keeps it `ready:false`; D10-7 hook row is `ok` at the plan-time doctor run (2026-09-14). | PROPOSED: every wave has exactly one batch (U1+U2 collapsed), launched by the orchestrator as one child + one independent verifier with the same collision/doctor checks | wave launch | n/a | P1 plan | CLOSED 2026-09-14 (width 2 from wave 1; DEC-C service-principal identity clears the D10-4 readiness consequence) | schedule width |
| D10-7b | D10 | Guard hook (`hooks.json` PreToolUse on `exec`) loads for the orchestrator's delegated worker shell (probe `__dbx_guard_probe__d5d8dc96` BLOCKED there, which is how the `ready=true` doctor report was obtained) but NOT for the orchestrator's own shell (same probe printed the echo twice, plain and tty, 2026-09-14). `migration-fanout/workflow.py` re-runs the doctor at launch with the probe result from the launching shell; an honest `not-blocked` fails the row and the workflow refuses every wave. | UNDECIDED: (a) platform fixes hook loading for the lead shell (reported to Cognition); (b) launch each wave from this session with the fan-out skill's "small wave" path (children + one fresh verifier launched directly, same collision check, `capabilities` contract, ledger gate by hand), children still run their own doctor + probe in their sessions | DECIDED (b), DEC-C3: direct-launch from the orchestrator session; children/verifier are separate sessions running their own doctor + probe; orchestrator applies collision/ledger/evidence gates per wave. (a) remains open with Cognition as a platform fix. | n/a | halt message 2026-09-14 | OPEN, blocks wave 0 launch | ACCEPTED (option b, DEC-C3); platform fix still pending |
| D10-7c | D10 | Guard hook does not load in child sessions either (child 16b12d4b probe nonce 290425d4 unblocked, 2026-09-14); option b's premise fails. | (a) platform fix / (c) accept `hook_platform_loaded` as known-fail row for children+verifier this run, other rows must pass | wave launch | n/a | halt message 2026-09-14 | OPEN, DECIDED (a) wait for platform fix (DEC-C5); re-check test in PARK-W0 | every wave |
| D10-12 | D10 | Tier 6 (CDC lag/ordering) needs the workspace to read Debezium topics; Kafka is host-internal only. Proposed: host-side sink writing to /Volumes/ow_tp/bronze/landing (parent implements); alternative: open 9092 to the serverless us-west-2 CIDRs. | parent | Tier 6 recon from wave 1 | n/a | halt message 2026-09-14 | OPEN | wave 1 |
