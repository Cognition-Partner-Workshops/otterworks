# 07_dependency_register

Four classes only. States: FOUND, DECIDED (owner and plan named), DONE.

| Id | Class | Object / finding | Owner | Plan | State |
|---|---|---|---|---|---|
| D1-1 | D1 Other writers | `services/legacy-billing` Flask app (`app/backends/oracle.py`, `app/facade.py`) is the only writer found: 13 PL/SQL calls + INSERT tenants/subscriptions/usage_events | app owner (customer) | migrates with the units; repoint at cutover (customer-held) | FOUND |
| D1-2 | D1 Other writers | Seed generator (`make oracle-billing-seed`) wrote batch 14531857; not a runtime writer | customer | excluded; no cutover action | DECIDED |
| D1-3 | D1 Other writers | Grants/sessions: `ow_billing` is the sole owner; no other grantee visible to `ow_billing_ro` (catalog partially hidden, D4-4) | customer DBA | confirm at STOP B that no other principal writes OW_BILLING | FOUND |
| D2-1 | D2 Other readers | `app/reports.py` (invoice_header x invoice_line, customer_master by batch) | app owner | migrates with units invoice_batch + customers (aggregation pipelines); Tier 4 ops | FOUND |
| D2-2 | D2 Other readers | `bridge/bridge.py` reads the app over HTTP (SQS/SNS), never the DB | app owner | none (unchanged by repoint) | DECIDED |
| D2-3 | D2 Other readers | No views, MVs, exports found (`ALL_VIEWS` 0; no `expdp`/report jobs in repo) | — | none | DECIDED |
| D3-1 | D3 Scheduled logic | `JOB_NIGHTLY_DUNNING` (02:00 daily, created DISABLED; `pkg_dunning.sp_schedule_dunning` + `sp_suspend_overdue`) | app owner | re-implement as scheduled Atlas Trigger / app cron in unit dunning; keep disabled on Oracle; enable on target only after STOP C | FOUND |
| D3-2 | D3 Scheduled logic | `JOB_PURGE_AUDIT_LOG` (03:30 daily, DISABLED; 90-day purge of BILLING_AUDIT_LOG) | app owner | retire with BILLING_AUDIT_LOG (0 rows); if kept, TTL index 90d | FOUND |
| D3-3 | D3 Scheduled logic | Triggers `trg_customer_master_hist`, `trg_subscriptions_hist` (row history, 0 rows produced) | app owner | retire with *_HIST tables (owner sign-off at STOP B) | FOUND |
| D3-4 | D3 Scheduled logic | Triggers `trg_*_seq`/`trg_billing_audit_log_id` (sequence assignment), `trg_sub_no_uncancel`, `trg_usage_events_check` (validation) | app owner | seq -> loader copies values, post-cutover counters (PROPOSED); validation -> app code + JSON schema validators in units subscriptions_rating | FOUND |
| D4-4 | D4 Access | `ow_billing_ro` lacks `SELECT_CATALOG_ROLE` (profile assessment tier): PL/SQL, trigger, sequence, job catalog rows invisible; census of those objects is repo-DDL-derived | customer DBA | optional: `GRANT SELECT_CATALOG_ROLE TO ow_billing_ro` and re-run `discovery.json`; otherwise accept DDL-derived rows (recommended: accept, fixture is built from that DDL) | FOUND |
| D4-1 | D4 Access | `ORACLE_BILLING_DSN` (app owner) over-scoped: CREATE JOB/TYPE/TRIGGER/PROCEDURE/SEQUENCE/VIEW/TABLE | customer DBA | superseded by `ORACLE_BILLING_RO_DSN` (probe_ok); credential not used | DECIDED |
| D4-2 | D4 Access | `MONGODB_MMP_RT_TARGET_URI` value is a 16-char token, not a connection string; target probe BLOCKED | customer / org secret owner | re-store the secret as a full `mongodb+srv://mmp_rt_target:...@<otterworks-demo>/mmp_rt_billing` URI, then re-run `!mongo_migrate` | DONE (2026-09-26 17:02, secret re-stored; target probe now reaches the cluster) |
| D4-3 | D4 Access | `MONGODB_MMP_RT_TARGET_URI` principal `mmp_rt_target` over-scoped: `readWrite@mmp_rt_billing_n` in addition to `readWrite@mmp_rt_billing`; target probe BLOCKED `privilege_excess` | customer / Atlas project owner | remove the `readWrite@mmp_rt_billing_n` role from `mmp_rt_target` (Atlas UI: Database Access -> mmp_rt_target -> Edit; or Admin API PATCH `/groups/<project>/databaseUsers/admin/mmp_rt_target` with roles = [readWrite@mmp_rt_billing]); then re-run `!mongo_migrate`. Alternatively a customer-signed risk-accept row naming the credential, the excess role, approver and expiry — Devin does not recommend this | DONE (2026-09-26, role removed by the customer; target probe_ok) |
