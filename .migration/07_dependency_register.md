# 07 Dependency register

Who else touches OW_BILLING. Four classes. Each row is FOUND, DECIDED (owner and plan
named), or DONE. Filled during the census (playbook 2); decided at STOP B.

| ID | Class | Finding | Evidence | State | Plan |
|---|---|---|---|---|---|
| D1-1 | Other writers | `billing-service` writes the transactional estate: `INVOICES`, `INVOICE_LINES`, `DUNNING_ATTEMPTS`, `NOTIFICATIONS`, `RATING_PERIODS`, `RATING_RESULTS`, `SUBSCRIPTIONS`, `CREDIT_NOTES` (via `PKG_INVOICING`, `PKG_RATING`, `PKG_DUNNING`) | `all_dependencies`; package bodies | DECIDED | Migration is a point-in-time copy of a quiet fixture. Coexistence window and repoint are cutover work (phase 4), owned by the customer; no dual-write is built. |
| D1-2 | Other writers | the CUSTBILL batch chain wrote the conversion feed (`INVOICE_HEADER`, `INVOICE_LINE`, `CUSTOMER_MASTER`, `ENTITY_ATTR_VALUE`) | `etl/legacy-extra`; `CONVERSION_BATCH_NO`/`BATCH_NO` columns | DECIDED | Feed is dormant in the fixture (no scheduler entry, no PL/SQL writer). Migrated as static history. If it runs again before cutover, the affected units re-run. |
| D2-1 | Other readers | the stored-proc parity harness reads the estate | `procs/harness/oracle_record.py` | DECIDED | Read-only; unaffected. Stays on Oracle until cutover. |
| D2-2 | Other readers | **RPT-114 month-end finance report** reads `INVOICE_HEADER` + `INVOICE_LINE` + `CODES` + `CUSTOMER_MASTER`, scoped by `batch_no`, decoding `INV_STATUS` through `CODES` and line types through a hard-coded `DECODE` | `services/legacy-billing/app/reports.py` | DECIDED | Served post-migration by `invoices` filtered on `source: "conversion"`; the 37 orphans fall out of its join today and are out of `invoices` tomorrow, so its numbers are unchanged. The hard-coded line-type decode is why `lines[].type` stays an integer. Repoint is customer work at cutover. |
| D3-1 | Scheduled logic | 5 PL/SQL packages (`PKG_OW_UTIL`, `PKG_PLANS`, `PKG_RATING`, `PKG_INVOICING`, `PKG_DUNNING`) hold the business rules | `all_objects`; package bodies | DECIDED | Out of scope for this data migration: logic extraction is the separate stored-procs track. Only the rules that shape data are carried: `f_str2dt`'s `DD-MON-YY`/NULL-on-failure semantics, `f_code_desc`'s `UNKNOWN(<n>)` fallback, and the `USAGE_EVENTS` check as a `$jsonSchema` validator. |
| D3-2 | Scheduled logic | 7 triggers. `TRG_CUSTOMER_MASTER_HIST` / `TRG_SUBSCRIPTIONS_HIST` write the history tables; `TRG_*_SEQ` assign sequence keys; `TRG_BILLING_AUDIT_LOG_ID` assigns `LOG_ID`; `TRG_SUB_NO_UNCANCEL` blocks un-cancelling; `TRG_USAGE_EVENTS_CHECK` validates units/kind | `all_triggers` | DECIDED | Data-shaping triggers are carried as data (`customer_history`, `subscription_history`) or as a validator (`usage_events`). Sequence triggers are not carried: the PRD's `_id` rule needs no counters. `TRG_SUB_NO_UNCANCEL` is a write-path rule for the new service, not a migration concern. |
| D3-3 | Scheduled logic | `JOB_NIGHTLY_DUNNING` (daily 02:00) and `JOB_PURGE_AUDIT_LOG` (daily 03:30, deletes `billing_audit_log` older than 90 days). **Both `enabled=FALSE`.** | `user_scheduler_jobs` | DECIDED | Dunning job: no data effect while disabled; its output tables are migrated as-is. Purge job: the source of the proposed 90-day TTL on `audit_log` (Open item 4) — needs the STOP B answer because the job is currently off. |
| D4-1 | Access | no read-only Oracle user exists on the fixture; the only account is the schema owner `OW_BILLING`, which can write | probed: `06_access_checklist.md` | DECIDED | **STOP A decision**: read-only is enforced by discipline (AGENTS.md rule 1) and the write-target allowlist, not by grants. On record. |
| D5-1 | Ownership | `SEGMENT_CD`, `REGION_CD`, `TERRITORY_CD`, `CHANNEL_CD`, `RATE_CLASS_CD` have no `CODES` rows and no reader anywhere in the estate or the applications; the last three are 100% NULL | `08_open_items.md` §5 | FOUND | Carried as integers, unchanged. Open against the billing data owner; decoding later is a one-pass update, not a re-migration. |
