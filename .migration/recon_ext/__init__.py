"""D13 recon adapter extensions (Postgres / DynamoDB) for the mongo-recon-harness.

The harness ships oracle/sqlserver/mongodb-atlas source adapters only. These modules add
source adapters implementing the harness `SourceAdapter` protocol; engine, tiers,
tolerances and report remain the harness's, unchanged. Recorded as PROFILE FEEDBACK.
"""
