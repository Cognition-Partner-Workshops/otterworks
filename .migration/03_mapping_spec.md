# MongoDB mapping specification

Version: `uninitialized`

This file is intentionally empty during phase 1. The approved Oracle census, access-pattern
evidence, field mappings, collection design, cardinality rules, element keys, indexes, and
unit/wave plan are owned by `!mongo_model` after STOP A.
