# 03 — Mapping spec

Initialized empty by `!mongo_setup`. Owned by `!mongo_model` (phase 2) and signed off at
STOP B. The machine-readable companion the recon harness consumes is
`.migration/03_mapping_spec.json`, produced by the same phase.

Nothing here is authoritative until STOP B is recorded in `05_decisions.md`.
