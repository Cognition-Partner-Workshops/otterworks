# 05_decisions — decision log

Every row: dated, who approved, provenance (`user:<id>`, `derived` with `file:line`, or `default-accepted` — the last never occurs in hard mode). Machine copy for `model_patch.py`: `05_decisions.json` (created in playbook 2).

| ID | Date | Decision | Evidence | Approved by | Status |
|---|---|---|---|---|---|
| D-000 | 2026-09-22 | `recon_mode: offline`, `stop_mode: hard`; source is DDL/PL-SQL only; target is local MongoDB `ow_billing_offline`; no Atlas, no MCP. | intake message in this session | user (intake) | DECIDED |
| D-001 | 2026-09-22 | Tolerance contract `tol-1` as tabled in `02_tolerances.md` (T1–T13, harness values 100000/1000/0/0/1). | Oracle profile `skills/mongo-migration/profiles/oracle.md` §type_mappings, §recon_canonicalization | customer, STOP A reply "Approved" (2026-09-22) | DECIDED |
| D-002 | 2026-09-22 | Empty string target policy = missing field (T7); NULL and missing compare equal (T8). | Oracle `'' IS NULL` semantics; profile §known_incompatibilities row 1 | customer, STOP A reply "Approved" (2026-09-22) | DECIDED |
| D-003 | 2026-09-22 | Text dates parse as `DD-MON-YY` English (T9) and `DD-MON-YY HH24:MI:SS` for `hist_dt` (T10); unparseable → missing + quarantine. | `services/legacy-billing/db/oracle/packages/01_pkg_util.sql:50-62`, `schema/01_tables.sql:218`, `etl/legacy-extra/tools/oracle_custbill_extract.py:19` | customer, STOP A reply "Approved" (2026-09-22) | DECIDED |
| D-004 | 2026-09-22 | Oracle `DATE`/`TIMESTAMP` are treated as UTC wall-clock on load (T3/T4). | no session TZ evidence in DDL; `schema/03_seed_static.sql` sets only NLS date formats | customer, STOP A reply "Approved" (2026-09-22) | DECIDED |
| D-005 | 2026-09-22 | `allowed_targets.json` carries `"catalogs": ["ow_billing_offline"]` next to `databases`. Intake asked for `"catalogs": []`, but the co-installed dbx-migration hook rejects an empty list ("must contain a non-empty 'catalogs' list") and blocked every file write in the repo until the key was non-empty; the same single local database name is carried so the allowlist stays one target. No Databricks catalog exists or is used. | intake message; hook source `hooks/dbx_guard.py:249-251`; tool rejections observed 2026-09-22 | customer, STOP A reply "Approved" (2026-09-22) | DECIDED (deviation) |
| D-006 | 2026-09-22 | Access probes for source RO principal, egress and query cap are recorded OPEN / NOT PROBED, not passed, because nothing is reachable offline. | playbook `1-setup.md` step 5 | user (intake) | DECIDED |
| D-007 | 2026-09-22 | Model `map-draft-2`: 16 collections, 3 embeds, all `cardinality_basis: derived`. Embeds: `RATING_RESULTS` → `ratingPeriods.results[]` (1:few); `INVOICE_LINES` → `invoices.lines[]` (1:few); `ENTITY_ATTR_VALUE WHERE ENTITY_TYPE='CUSTOMER'` → `customers.attributes[]` (1:few). `INVOICE_LINE` stays a collection referenced from `legacyInvoices` (orphans exist by contract). Renames: `customers`, `customerVersions`, `subscriptionVersions`, `legacyInvoices`, `legacyInvoiceLines`. Patterns: `ttl` on `billingAuditLog` (90 d), `document_versioning`/`history_copy` on the two `*Versions`, `attribute` on `customers`, `bucket` (candidate only) on `usageEvents`. 90 machine decisions, each with `file:line` or a STOP A quote. | `05_decisions.json`; `packages/03_pkg_rating.sql:88,199-209`; `packages/04_pkg_invoicing.sql:150-157`; `app/facade.py:286-299`; `app/reports.py:67-68`; `docs/tech-partnerships/billing-report-contract.md:30-31` | derived; pending STOP B | PROPOSED |
| D-008 | 2026-09-22 | Text-date formats: `%d-%b-%y` for every `VARCHAR2(9) *_dt` incl. `UDF_DT_01..10` (T9, `assume_text_date` because the DDL has no comment on the UDF columns); `%d-%b-%y %H:%M:%S` for both `HIST_DT`. | `schema/02_horror.sql:4,63-67,163-172,225-229,370,385,420,434-435`; `schema/01_tables.sql:218`; STOP A "Approved" (T9/T10) | customer (T9/T10) + derived | DECIDED |
| D-009 | 2026-09-22 | PL/SQL packages, business triggers and scheduler jobs are not migrated as data: `pkg_*` logic and `trg_sub_no_uncancel` / `trg_usage_events_check` are ported as application logic (register D3 rows); history triggers become explicit writes to the `*Versions` collections; `JOB_PURGE_AUDIT_LOG` → TTL index; `JOB_NIGHTLY_DUNNING` → service scheduler, owner to be named at STOP B (U-5). | `schema/04_jobs.sql:8-27`; `schema/01_tables.sql:210-252`; `schema/02_horror.sql:360-375` | derived; pending STOP B | PROPOSED |
| D-010 | 2026-09-22 | `schema/04_upgrade_static.sql` (two unparsed statements: `fixture_meta` marker maintenance, lines 6 and 170) carries no data-model content and is excluded from the offline Oracle fixture because it depends on its own bootstrap table. It stays in the census as unparsed. | `.migration/census.json` unparsed[]; intake message | user (intake) | DECIDED |
| D-011 | 2026-09-22 | Units and waves: 7 units / 16 collections, run sequentially in this session (width 1; under 10 units and fixture-sized data, playbook 2 step 5), `auto_merge: false` (hard mode). Wave 0 `codes`; wave 1 `tenancy-plans`, `customers` (XL), `legacy-invoice-feed`; wave 2 `usage-rating`, `invoicing`, `dunning-ops`. | `.migration/waves/wave-{0,1,2}.json` | pending STOP B | PROPOSED |
| D-012 | 2026-09-22 | `FLAG_01..20 CHAR(1)` stay strings (rstrip, empty → missing): the DDL gives no domain and no code reads them; T5 covers `*_yn` only. Reopen if STOP B says they are Y/N. | `schema/02_horror.sql:93-112`; no reader found under `services/`, `frontend/`, `etl/` | derived; pending STOP B (U-4) | PROPOSED |
