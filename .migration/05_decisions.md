# 05_decisions: decision log

Provenance is `user:<message/event id>` only when a human replied; `default-accepted (soft, 60s, no reply)` when a soft window elapsed; `intake` for facts stated in the kickoff message; `orchestrator` for automatic, rule-mandated actions.

| ID | Date (UTC) | Decision | Approver / provenance | Notes |
|---|---|---|---|---|
| D-000 | 2026-09-26 | Engagement facts as in `00_context.md`: offline policy, `stop_mode` soft for A/B, STOP C hard, width 3, no reviewer, `auto_merge: false` everywhere, no PR merged on offline evidence, branch restriction to `tech-partnerships` + run branch | intake (kickoff message, session 983733d4647d4ea2b1fd4344adfa7b65) | |
| D-000a | 2026-09-26 | `allowed_targets.json` carries a `catalogs` key mirroring the single `databases` entry `ow_billing_migration`, because the co-installed dbx-migration-factory PreToolUse hook parses the same file and rejects any tool call while it lacks a non-empty `catalogs` list. No Databricks target is authorized; the mongo kit reads `databases` only | orchestrator (tool refusal recorded verbatim in `06_access_checklist.md`) | deviation from playbook 1 step 8 wording ("only the designated migration database"): same single database, extra key |
| D-001 | 2026-09-26 | STOP A: tolerances `tol-1` accepted as proposed (exact; row-diff threshold 100000; sample 1000; source concurrency 1; null_missing_equiv); access axes `source_access=ddl_only`, `target_access=local` (policy offline; nothing BLOCKED). Grading-only amendments pre-authorized | default-accepted (soft, 60s, no reply) | posted ~16:52 UTC in session 983733d4647d4ea2b1fd4344adfa7b65; no human reply in window |
| D-002 | 2026-09-26 | Change request received at 17:0x UTC, well after the STOP A 60 s window: "set sample size to 2000 instead of 1000". Not applied (a late reply is a change request at the next stop, never a silent change). Carried to STOP B as an explicit option; `tol-1` (sample 1000) stays in force until a human reply at STOP B applies it | pending: user message (late, post-window) | tolerance change ⇒ needs a human reply; the STOP B soft default does not apply it |
| D-003 | 2026-09-26 | Mapping spec `map-draft-2` produced only through `05_decisions.json` (68 cited decisions) + `model_patch.py`; 16 collections, 3 derived embeds (INVOICE_LINES→invoices.lines 1:few; ENTITY_ATTR_VALUE→customerMaster.attributes 1:few scoped `entity_type='CUSTOMER'`; INVOICE_LINE→invoiceHeader.lines 1:N with orphan quarantine); `modeling.unresolved` = 0 | orchestrator (playbook 2 step 4; every cite in `05_decisions.json`) | pending human confirmation at STOP B |
| D-004 | 2026-09-26 | Scope: 19 tables → 18 unit + 1 shared (CODES); 0 proposed-unused; excluded = 5 sequences, 2 identity triggers, FIXTURE_META (+2 unparsed statements), the Postgres original of the estate. Register D1-1..4, D2-1..4, D3-1..3 DECIDED with plans; D4-1..3 FOUND/DECIDED, customer-owned | orchestrator (`09_coverage.md`, `07_dependency_register.md`) | pending STOP B |
| D-005 | 2026-09-26 | Fixture: same-engine synthetic Oracle Free container seeded `NS=demo` (~203k rows), manifest `fixtures/ow_billing_demo.json` (method synthetic, masked_columns [] – nothing to mask, produced_at/by, row_counts). Non-merge evidence | orchestrator | fixture PASS never merges (rule 8/11) |
| D-006 | 2026-09-26 | Waves: 12 units in 4 waves (0: codes single-session; 1: 3 batches fan-out; 2: 3 batches fan-out; 3: 2 batches single-session), width 3, XL = u-02, u-06; every manifest `auto_merge: false` (offline evidence), validated by the fan-out manifest/collision/fixture gates | orchestrator (`10_wave_plan.md`, `waves/*.json`) | pending STOP B |
| D-007 | 2026-09-26 | STOP B: model `map-draft-2`, scope (`09_coverage.md`), register plans, fixture, wave plan (4 waves, width 3, `auto_merge: false`) accepted as proposed; tolerances stay `tol-1` (sample 1000) — the late sample-2000 request (D-002) was offered and not taken up by a human reply, so it is not applied | default-accepted (soft, 60s, no reply) | posted 17:10 UTC minus 60 s in session 983733d4647d4ea2b1fd4344adfa7b65; no reply in window |
| D-008 | 2026-09-26 | Fan-out mechanics: `run_workflow` passes no environment and starts in `$HOME`, so `workflow.py` cannot read `WAVE_MANIFEST`/`WAVE_RUN_ID` as documented. Added `waves/run_fanout.py`, a launcher that sets them (run id from the shim's `_DEVIN_WORKFLOW_RUN_ID`) and executes the unmodified `workflow.py`; guards/breaker/verifier untouched. Child briefs extended with fixture-bootstrap steps because separate-VM children have no shared local fixture (scope, width, targets unchanged; STOP B inputs unaffected) | orchestrator (playbook 0 step 5 could not run as written) | tooling deviation, not a migration decision |
| D-009 | 2026-09-26 | Wave 1 closed by the fan-out workflow (run wfr-c59acd166c8443bfa04f18fb7c737411): 0/3 PASS by rule — w1-b01/w1-b03 `offline_no_live_recon` (fixture recon PASS, expected offline outcome), w1-b02 `tolerance_ambiguous`; verifier skipped (no PASS batch); no breaker, no collision, no undeclared target. PRs #1710/#1711/#1712/#1716 stay open, unmerged | workflow result file `waves/wave-1.result.json` | merge policy unchanged (D-000) |
| D-010 | 2026-09-26 | Pending tolerance decision from u-02-customers: the Oracle profile rules `date_string_to_date`/`csv_to_array` grade unparseable planted values raw while D-0xx in `05_decisions.json` mandates quarantine, so Tier 3 shows exactly the 63 quarantined values as diffs. Recommended fix: profile rule param `unparseable: quarantine` (grader treats a quarantined source value vs missing target field as equal, counted in the quarantine rate) — a grading rule change, i.e. tolerance record `tol-2`. Not applied: tolerance changes need a human reply | pending: human reply to the wave-1 close message | hard by rule (changes tolerances) |
| D-011 | 2026-09-26 | tol-2 approved: quarantine-aware grading. Implemented as engagement profile overlay `profiles/oracle.md` (`date_string_to_date.unparseable=null`), `02_tolerances.json` version tol-2; `csv_to_array` has no such param in recon 0.3.2 so 13 malformed_csv values remain graded as diffs (harness gap, reported as skill feedback). u-02-customers recon to be re-run under tol-2 in a fresh session; wave 2 (already launched under tol-1 profile) is unaffected because none of its rules changed | user: chat reply "approve tol-2 quarantine-aware grading" (wave-1 close) | hard stop answered by human |
| D-012 | 2026-09-26 | Wave 0 closed: single-session path (1 batch) + fresh reconciliation session devin-a43ac910a9f44615ad85cd776a72e636; verifier fixture PASS on local target, live recon pending customer run; w0-b01 recorded FAIL-by-rule offline_no_live_recon; PR #1708 open, unmerged | orchestrator, `waves/wave-0.result.json`, `recon/wave-0/report.md` | merge policy unchanged |
| D-013 | 2026-09-26 | Wave 2 closed by the fan-out workflow (run wfr-2383174fee8842759d52d1d2375a5d9e): 0/3 PASS by rule — w2-b01 BLOCKED-by-rule/w2-b02 FAIL `offline_no_live_recon` (fixture recon PASS), w2-b03 `tolerance_ambiguous`; verifier skipped; no breaker, collision or undeclared target. PRs #1721/#1722/#1724 open, unmerged | `waves/wave-2.result.json` | merge policy unchanged |
| D-014 | 2026-09-26 | Pending modeling decision from u-06-invoice-header-bulk: Tier 1 embed_cardinality counts all 150,000 INVOICE_LINE rows, but the 37 orphan rows (no parent INVOICE_HEADER; planted by the fixture) are quarantined per the accepted model. Recommended fix: add `child_where: "invoice_id IN (SELECT invoice_id FROM INVOICE_HEADER)"` to the invoiceHeader.lines embed via a cited row in `05_decisions.json` + `model_patch.py` (spec map-draft-3). Spec change = human reply; not applied | pending: human reply to the wave-2 close message | hard by rule (changes scope of a gate) |
| D-015 | 2026-09-26 | tol-2 first re-run (session devin-8f39d92539f3401bb43ed01c23ee1028) still 63 diffs: the overlay's `unparseable=null` did not reach the spec alias `date_string_to_date:dby-b3d57e` (exact-name merge). Overlay now declares both spec aliases; annotation keys moved out of `02_tolerances.json` (harness rejects unknown keys). Values of tol-2 unchanged; re-run ordered | orchestrator; implements D-011 | no new tolerance |
| D-016 | 2026-09-26 | tol-2 applied and verified on u-02-customers (session devin-8f39d92539f3401bb43ed01c23ee1028, run 3/3): the 50 bad_date diffs clear; 13 malformed_csv diffs remain because recon 0.3.2 `csv_to_array` has no quarantine-aware param. u-02 stays FAIL `harness_gap_csv_unparseable`; no further tolerance change proposed (a rule that ignored all CSV mismatches would widen the gate beyond what was approved). Reported as skill feedback for the plugin | orchestrator; evidence PR #1716 @79d0365 | merge policy unchanged |
| D-017 | 2026-09-26 | Process incident: one orchestrator commit (9a3e8988, .migration only) was made while the shared checkout sat on the w3-b01 batch branch. Corrected without force-push (guard forbids it): run branch fast-forwarded to 9a3e8988; batch branch carries 9a3e8988 + its revert 063c98d3 (net zero `.migration/` change, verified by diff). Orchestrator now commits from a dedicated worktree of the run branch | orchestrator | none |
| D-018 | 2026-09-26 | map-draft-3 approved: `invoiceHeader.lines` embed scoped with `child_where: invoice_id IN (SELECT invoice_id FROM invoice_header)` / `target_where: {"lines.invoiceId": {"$exists": true}}` (71 cited decisions, `model_patch.py` from the census proposal; only the embed scoping differs from map-draft-2, `unresolved` = 0). Noted per the approver: a where-scoped embed makes the harness report `merge_eligible=false` (known harness limitation) — immaterial here because offline/local evidence never merges. u-06-invoice-header-bulk recon to be re-run under map-draft-3 in a fresh session | user: chat reply "approve map-draft-3 child_where" (wave-2 close) | hard stop answered by human |
| D-018a | 2026-09-26 | Correction within D-018: the u-06 re-run under map-draft-3 FAILED Tier 1 (embed cardinality source 149,963 vs target 0, evidence fdf05f7f on PR #1724) because `target_where` keyed on `lines.invoiceId`, a field the accepted loader never persists (INVOICE_ID is the parent `_id`). Regenerated as `map-draft-3.1` with `target_where: {"lines.lineNo": {"$exists": true}}` (LINE_NO is on every embedded element); `child_where` unchanged, no other spec change. u-06 re-run #2 launched. Note: run-branch `02_tolerances.json` is the tol-2 file (D-011); re-runs use it as the effective tolerance | orchestrator (implementation detail of the human-approved D-018; scope unchanged) | — |
| D-019 | 2026-09-26 | u-06 re-run #2 under map-draft-3.1 PASS at every tier (embed cardinality 149,963 = 149,963, 168,713 keyed checks, 0 diffs); the 37 orphan INVOICE_LINE rows stay quarantined (`orphan_line`) and counted, not embedded. Harness notes: where-scoped embed warns "extra target elements not checked" and never reports merge_eligible=true (known limitation, D-018); harness rejects `--allowed-targets-file` outside `<root>/.migration/` (worktree copy used, byte-identical). Wave 2 `tolerance_ambiguous` on u-06 is resolved; PR #1724 remains unmerged (offline evidence, auto_merge:false) | orchestrator (result of user-approved D-018) | — |
| D-020 | 2026-09-26 | Wave 3 closed on the single-session path (2 batches, playbook 0 step 5): w3-b01 PR #1725 and w3-b02 PR #1726 opened against the run branch, fixture recon PASS on every unit, independent verifier (session 345d39d8, branch --recon-w3, c961af93) agrees tier-for-tier: "fixture PASS, live recon pending customer run". Both PRs stay unmerged: `auto_merge: false`, local-target evidence never merges (rule 11). Verifier findings F1/F2 (low, unreachable) and D1 (subset version label) go to the cutover open-issues list, not a halt (no gate failed, no collision, breaker not tripped) | orchestrator | — |
| D-021 | 2026-09-26 | Phase 4 cutover preparation (playbook 5 steps 1-4). Step 1 final delta catch-up NOT executable: no live source, no migration cluster (`08_connectivity.json` ddl_only/local); drafted as runbook §2 for the customer DBA. Step 2 evidence pack `cutover/evidence_pack.md`: INCOMPLETE (no parallel-run log, no watermark recon, no live/snapshot evidence, 0/12 units merge-eligible, no D1-D3 row DONE). Step 3 independent audit by session 743077ac (migrated nothing), `cutover/audit.md` @ fe3648ad: STOP C BLOCKED, agrees INCOMPLETE, 13 findings (F-01/F-02 blocker = missing customer run / nothing repointed; F-03 subset version labels; F-04 units u-00..u-05 gated under map-draft-2/tol-1 never re-gated under 3.1/tol-2, stale tol-1 titles in 00_context/02_tolerances corrected here; F-05..F-11 medium/low, all carried in `cutover/open_issues.md`). Step 4 runbook `cutover/runbook.md`: read-consumer cutover only, every production step customer-executed, rollback config-only, PNR = first repointed write. Phase outcome offered: `read-consumer cutover`; `full retirement` not offered (every PL/SQL object still executes on source) | orchestrator | — |
| D-022 | 2026-09-26 | STOP C posted (hard, always). Per playbook 5 "Before you start": `source_access: ddl_only` / `target_access: local` — fixture or local-target evidence never satisfies STOP C. Cutover authorization REFUSED/BLOCKED; missing customer run named: harness `--mode live|snapshot --target-class migration-cluster` for u-00..u-11, run in-network by the customer DBA (D4-1, D4-2), plus D4-3 counts and D2-4 reader confirmation. No production repoint executed or scheduled. This block is the expected outcome of the offline engagement; no default, no workaround. Resume rule: a later `!mongo_migrate` re-derives phase 4 and re-posts STOP C only if the evidence pack changes | orchestrator | blocked, awaiting customer evidence |
