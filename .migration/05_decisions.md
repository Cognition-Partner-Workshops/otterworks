# 05 — Decision Log

Append-only. Every STOP outcome, tolerance version, grading-only amendment, halt, and
orchestrator phase transition is a dated entry with an owner.

| # | Date (UTC) | Owner | Entry |
|---|---|---|---|
| 1 | 2026-09-01 20:52 | orchestrator | Phase 1 (`!mongo_setup`) started. `.migration/` absent on `tp-run/mongodb-20260901T205236Z` → new engagement. Prior run `tp-run/mongodb-20260901T032752Z` used as structural reference only; none of its approvals carry over. |
| 2 | 2026-09-01 21:05 | orchestrator | Environment probed: Atlas preflight 8/8 verified; Oracle/Postgres/DynamoDB fixtures booted and seeded (`NS=demo`), read probes WORKS (see 06). Workspace committed. Routing → STOP A (awaiting approval; tolerance record v1 PROPOSED). |
| 3 | 2026-09-01 21:12 | dhrov.subramanian (requester) | **STOP A APPROVED** as proposed ("Approved", Slack thread p1788296321439879 in #ow-migrations). Tolerance record **v1 = FACT**; target `ow_tp_mongodb_205236` / ns `mongo_205236` = FACT; recon LIVE, source-load cap 1, rerun cap 3, 3-cycle parallel-run window; source topology VM-local. Grading-only amendments pre-authorized. |
| 4 | 2026-09-01 21:12 | orchestrator | Routing → Phase 2 (`!mongo_inventory_and_model`). |
| 5 | 2026-09-01 21:42 | orchestrator | Phase 2 census complete (read-only): Oracle 20 tables/432 cols/17 PL/SQL objects/7 triggers/5 seqs/2 jobs, Postgres 3 tables, DynamoDB 1 table, 24 recorded PL/SQL transcripts. Mapping spec **v1.0-proposed** authored (18 collections, 419 root + 48 embedded field mappings, 5 graded embeds; `03_mapping_spec.json` loads in the recon harness); coverage table 44/44 objects bucketed (1 excluded: `FIXTURE_META`); decisions D1–D13 PROPOSED; units U0–U9, waves 0–3; write targets declared PLANNED in 04. Width recommendation: full fan-out (Option 1). Committed. Routing → **STOP B** (chat + #ow-migrations). |
| 6 | 2026-09-01 21:55 | dhrov.subramanian (requester) | **STOP B APPROVED** as proposed ("approved", Slack thread p1788296971419259 in #ow-migrations). Mapping spec **v1.0-proposed → v1.0 (FACT, frozen)**; decisions D1–D13 = FACT; units U0–U9, waves 0–3 = FACT; width = **Option 1 (recommended default): full fan-out via the dynamic workflow**, one child per unit, children re-boot the deterministic `NS=demo` fixture and self-verify `run_mode: fixture`; parent runs the single LIVE independent wave recon (cap 1). Not width-1-inline. |
| 7 | 2026-09-01 21:55 | orchestrator | Routing → Phase 3 (fan-out dynamic workflow). Write targets flip PLANNED→REGISTERED per unit at contract time. |
| 8 | 2026-09-01 22:20 | orchestrator | Wave 1 PARTIAL CLOSE: U3 (#1420 @ `dfa5e978`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave1-recon:.migration/recon/wave_reports/wave1.md`). **U4 (#1419) HELD, not merged**: the recon report verified branch `--u4` @ `51f7cca3`, but the PR head advanced to `80eff2aa` ("review round 2": `load_u4.py` staging-collection + rename, evidence regenerated in fixture mode) which the LIVE recon does not attest to. Requires either a LIVE re-recon of `80eff2aa` or an explicit orchestrator waiver before merge; wave 1 remains OPEN. Grading amendments: none applied. Suggested (not applied): (1) record operative `version_gaps` definition `1..max(documents.version, max(version_number))` to disambiguate spec text; (2) PROFILE FEEDBACK: harness does not grade BSON type width (`long` vs `int`) — a Tier-2 `$type` histogram check would catch flag C; (3) decide estate-wide whether D2 'explicit BSON null' is required (U3 needs a one-line loader change pre-cutover) or 'missing' is acceptable under `null_missing_equiv` (then loosen D2 wording). |
| 9 | 2026-09-01 22:55 | orchestrator | Fan-out launched: dynamic workflow run `wfr-2defeb1f434347a8a9897c0bea356bcc` (journal: this row + `07_fanout_journal.md`). Wave 0 U0 child (devin-4a712cd3cdda4e22add668ce6fa915ca) escalated a **grading-only amendment**: harness Tier-3 compares source key tuples to the scalar target `_key`, so `codes` key.source is amended from `[CODE_TYPE, CODE_VAL]` to the single expression `CODE_TYPE\|\|':'\|\|CODE_VAL` (target `_key` unchanged). No document shape, tolerance, or data changed → pre-authorized per STOP A; applied as mapping spec **v1.0.1**. U0 child resumed by message (not relaunched). U4 (PR #1419) held at wave-1 merge because its head (80eff2aa) post-dates the LIVE-reconned SHA (51f7cca3): resolution = parent LIVE re-recon of the head in the next wave-1 recon pass (no waiver). U3 (PR #1420) merged on LIVE PASS (row #8). |
| 10 | 2026-09-01 22:42 | orchestrator | Wave 1 CLOSED: U3 (#1420 @ `dfa5e978`, merged earlier, row #8), U4 (#1419 @ `3420f475`, merged `cdb0abd4`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave1-recon:.migration/recon/wave_reports/wave1.md`, pass 2 re-attested both current heads); grading amendments: none |
| 11 | 2026-09-01 22:57 | orchestrator | Wave 0 CLOSED: U0 (#1423 @ `892eb88a`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave0-recon:.migration/recon/wave_reports/wave0.md`); grading amendments: none |
| 12 | 2026-09-02 00:30 | orchestrator | Wave 1 PARTIAL CLOSE (part 1 recon): U1 (#1430 @ `c5baa80a`, merged `e87de328`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave1-recon-part1:.migration/recon/wave_reports/wave1.md` @ `475560d4`; U3 #1420 and U4 #1419 re-attested at current heads, already merged rows #8/#10). **U2 (#1432 @ `9643ce76`) BLOCKED, not merged**: LIVE PASS attested, but `git merge` into the run branch after U1 conflicts substantively in `services/legacy-billing/app/reports.py` (U1 rewrote RPT-114 BALANCES path, U2 rewrote STATUS/LINE paths — both edit imports/helpers), `services/legacy-billing/tests/test_reports.py`, `services/legacy-billing/app/requirements.txt` (pymongo 4.13.2 vs 4.10.1) and `.migration/04_progress.md`. Merge aborted; no conflict resolved by the merge agent. Resolution = U2 child rebases `--u2` onto the run branch (post-U1) and the parent LIVE re-recons the new head before merge; wave 1 remains OPEN. Grading amendments: none applied; recommended (not applied): (1) pin the 02_tolerances quarantine-ceiling denominator so U3's 6/390 is unambiguous; (2) Tier-2 BSON `$type` histogram per declared `bson_type` (F-U4-1). |
| 13 | 2026-09-02 00:59 | orchestrator | Wave 2a CLOSED: U5 (#1438 @ `1aefd226`, merged `944c9dcb`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave2a-recon:.migration/recon/wave_reports/wave2a.md`); grading amendments: none |
| 14 | 2026-09-02 02:25 | orchestrator | Wave 2b CLOSED: U6 (#1440 @ `f463577b`, merged `a793f45c`), U7 (#1439 @ `f05741f3`, merged `fbe9aec2`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave2b-recon:.migration/recon/wave_reports/wave2b.md`); grading amendments: none applied. Described only, orchestrator decides: (1) optional re-include `billing_audit_log` in U7 `GRADED_SOURCES` now that the target holds the observer row (1/1 equal; additive coverage, no shape/rule/tolerance change); (2) optional mapping-spec clarification declaring the `counters` document contract (`_id` casing, seq field, ns, source_sequence) so U6/U7/U8/U9 converge (F-X-1). Merge note: the U7 merge conflicted only in `.migration/04_progress.md` (both children edited adjacent register/unit rows); resolved as a plain union, no code conflicts. |
| 15 | 2026-09-02 03:59 | orchestrator | Wave 3 CLOSED: U8 (#1447 @ `0024b45e`, merged `1d7aa08b`), U9 (#1444 @ `9f67ec79`, merged `24592b0b`) merged on independent LIVE recon PASS (report `tp-run/mongodb-20260901T205236Z--wave3-recon:.migration/recon/wave_reports/wave3.md`); grading amendments: none applied (none warranted). Described only, orchestrator decides: (1) both gates grade the loaded clone only and Tier-4 by transcript return values — a post-Tier-4 Tier-3 diff vs an Oracle replay would have caught F-U8-1 (rebuilt `lines[]` omit `invoice_id`) mechanically (driver enhancement, not a tolerance change); (2) U8's driver does not reset its clone after Tier-4 (U9's does), so any second gate on the same load fails Tier-1 — false alarm only, a `--reset-after` would remove it. Open before cutover: F-U8-1 one-line fix + re-gate, F-X-1 `counters` contract (now three call sites), F-U8-2/F-U7-1 behaviour decision. Merge note: the U9 merge conflicted only in `.migration/04_progress.md` (both children edited adjacent register/unit rows); resolved as a plain union, no code conflicts. |
