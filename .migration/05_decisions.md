# 05 Decisions

The record of every stop and every amendment. A decision that is not a row here does not
exist, whatever the chat says. Provenance is `user:<message id>` only when a human replied;
silence in soft mode is `default-accepted (soft, 60s, no reply)`.

| # | Date | Decision | Provenance | Notes |
|---|---|---|---|---|
| 1 | 2026-09-15 | Engagement opened. `stop_mode: soft`, STOP B hard, STOP C always hard. PRD v1.0 is the binding target model. | user:engagement request | `00_context.md` |
| 2 | 2026-09-15 | **STOP A accepted.** Tolerances v1 as written in `02_tolerances.json` (exact numeric/date, money aggregates 0.01, row threshold 100000, source concurrency 2, rerun cap 3, `null_missing_equiv`, empty string → `null`). Anomaly budget frozen at 37/50/31, compared as a set; any deviation is a finding, never a tolerance change. Recon LIVE, parent-only; children `fixture` mode. Fan-out width 4. Grading-only amendments pre-authorised. | default-accepted (soft, 60s, no reply) | STOP A posted 2026-09-15; no reply in window |
| 3 | 2026-09-15 | **Read-only posture accepted as a discipline, not a grant.** No read-only Oracle account exists (`OW_BILLING` schema owner only, write-capable); creating one would write to the source. Mitigations in `06_access_checklist.md` A2. Carried as D4-1. | default-accepted (soft, 60s, no reply) | requester asked for this on record |
| 5 | 2026-09-15 | **STOP B approved** ("Go with proposals. Approved"). Model = the 13 PRD collections in `03_mapping_spec.json`, nothing added. Open items resolved as proposed: (1) drop the 80 null `FLAG_*`/`UDF_*` columns; (2) `usage_events` a standard collection, not time-series; (3) one `invoices` collection with a `source` discriminator (`conversion`/`billing`); (4) 90-day TTL on `audit_log` only, none on customer/subscription history; (5) `segment`/`region`/`territory`/`channel`/`rateClass` stay integers, ownership unresolved (D5-1). Scope: wave 0 reference data (1 batch, in-session), wave 1 four units via `migration-fanout` at width 4. `subscription_history` grouped into wave 0. Three canonicalisation rules raised as profile feedback, not harness patches. | user:stop-b-reply-2026-09-15 | evidence in `08_open_items.md`, `census.md` |
| 6 | 2026-09-15 | **Grading-only amendment (pre-authorised at STOP A).** `codes` compares on the decomposed pair (`codeType`, `codeVal`) instead of the composed `_id`; the harness requires equal-length key tuples. PRD identity `_id = CODE_TYPE:CODE_VAL` is unchanged. Fields the harness has no rule for (Y/N, CSV, `DD-MON-YY` strings) and CODES-decoded fields are graded by tier 4 operations instead of tier 3. | grading-only amendment, pre-authorised (STOP A) | `03_mapping_spec.json`, `waves/wave-0.result.json` |
| 4 | 2026-09-15 | **Working branch `tp-run/mongodb-20260915T045208Z`**, cut fresh from `tech-partnerships`. The branch named at intake did not exist; every older `tp-run/mongodb-*` already carries a finished run. | default-accepted (soft, 60s, no reply) | reversible: reply `use <branch>` and the workspace moves |
| 7 | 2026-09-15 | **Wave 0 merged into the run branch.** PR #1563 (U1-reference) merged after live recon PASS, `make tp-smoke` green and three review findings fixed: snapshot-consistent source reads, source-key-set convergence, schema-valid recon artifact. Soft mode merges PASS PRs as they are verified. | default-accepted (soft) | `waves/wave-0.result.json` |
| 8 | 2026-09-15 | **Wave 1 ran through `migration-fanout`**, run `wfr-c213ed80fea946808f2878e1d4356c5a`, 4 batches at width 4. Breaker never tripped, no write-target overlap or undeclared target. All four batches PASS; the independent verifier re-ran live recon over all four and merged PRs #1564, #1565, #1567, #1568. Anomaly budget matched exactly: 37 orphaned invoice lines, 50 unparseable `SIGNUP_DT`, 31 malformed CSV lists. | workflow result | `waves/wave-1.result.json`, `waves/wave-1.brief.md` |
| 9 | 2026-09-15 | **Wave 1 verifier verdict recorded as a reporting-shape failure, not a unit failure.** The verifier keyed its verdicts by collection (`customers`, `invoices`, …) where the workflow expects batch ids (`w1-b01`…), so the result file says FAIL with `closed: false` while every finding in it is a PASS. Unit evidence stands on the four live recon runs and the verifier's own re-run. Folded into skill feedback: the verify prompt must require batch-id keys. | default-accepted (soft, 60s, no reply) | `waves/wave-1.result.json` lines "verifier output invalid" |
