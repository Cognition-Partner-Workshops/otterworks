# Decision log

Provenance is `user:<message/event id>` (a human replied) or `default-accepted (soft, 60s, no reply)`.
Never `user:` without a human reply; never a default at STOP E.

| ID | Date | Decision | Options | Recommended | Outcome | Provenance | Forced by | Blast radius |
|---|---|---|---|---|---|---|---|---|
| DEC-000 | 2026-09-14 | Intake consumed as FACT from the parent's confirmed intake (`ow_billing_intake_draft.md`); `stop_mode: soft` | — | — | recorded | parent intake (confirmed with customer 2026-09-14) | front door | whole engagement |
| DEC-A | 2026-09-14 | STOP A: target profiles, track split, tolerances `tol-p1-v1`, access posture and D10-1..6 as written in `stops/STOP_A.md` | approve as-is / amend rows | approve as-is (D10-4 option (a): PAT identity accepted for this run; D10-7 option (a): convention-only write-scope enforcement; rehearsal fallback freeze-and-load until D10-1 closes; `cdc_lag_max_s: 60`; Debezium kept as the CDC path per intake) | APPROVED 2026-09-14 with amendment: CDC coexistence is primary (Debezium on the existing host), freeze-and-load is fallback only; PAT identity accepted; convention-only write guard; `cdc_lag_max_s` 60. Customer's exact words (relayed by parent): "Approve STOP A with CDC coexistence: Debezium on the existing host is primary, freeze-and-load is fallback only, PAT identity accepted, convention-only write guard, cdc_lag_max_s 60" | parent relay of customer reply | setup | all downstream |
| DEC-A1 | 2026-09-14 | Parent facts recorded after STOP A (FACT, no re-probe of Oracle DDL): D10-1 DONE by the parent as the customer's delegate (ARCHIVELOG, `SUPPLEMENTAL_LOG_DATA_MIN=YES`, `c##dbzuser` with the Debezium LogMiner grant set, ALL COLUMN logging on the 10 operational tables); Debezium credential = Secrets Manager `ow-tp/oracle/dbzuser` (Debezium use only, never printed); D10-6 CLOSED (`ow_billing_ro` rotated, re-fetch before every Oracle read); D10-2 CLOSED (`sg-0eaf11f4434260e1e` admits TCP 1521 from the us-west-2 serverless egress CIDRs); D10-4/D10-7 ACCEPTED for this run; Debezium + Kafka + Kafka Connect run as containers on EC2 `i-0be201ad6412c5e5` (52.201.36.9), deployable via SSM (instance role reads `ow-tp/oracle/dbzuser`), no other change to the host's Oracle container. | — | — | recorded | parent message | STOP A | operational track |
| DEC-B | 2026-09-14 | STOP B: first pipeline. Customer's exact words (relayed by parent): "Approve STOP B: first pipeline P1 Monthly invoicing." | P1 Monthly invoicing (wave 0 shared set first) | P1 | approved (explicit, before the soft-stop default fired) | parent relay of customer reply | STOP B | P1, P2 ordering |
| DEC-B1 | 2026-09-14 | D4-5 consumer model, FACT (customer-stated), verbatim: "The system appears incomplete, so the migration should define the intended model rather than guess missing callers: - Billing service: owns plan changes, usage rating, invoice creation, credits, and dunning. - Monthly scheduler: tells the billing service when to close a billing period and issue invoices. - Product services: produce usage events when customers consume billable resources. - Usage-ingestion service: validates and deduplicates those events before writing USAGE_EVENTS. - Reports: read billing data but never write to it. The migration should remain blocked on production cutover until the customer identifies the billing service and usage-event producers and demonstrates: usage event -> rating -> invoice -> notification. The test harness and seed scripts are not real production callers." Consequence: STOP E hard precondition (cutover blocked until the billing service and usage-event producers are named and the usage event -> rating -> invoice -> notification path is demonstrated end to end); the Lakebase application surface (PL/pgSQL procedures, sequences, constraints) is designed for the billing service above; no callers invented. | as stated | D4-5, D3-1, D7-1 | recorded | parent relay | STOP B | STOP E precondition; P1 design |
| DEC-B2 | 2026-09-14 | D10-8: approved; parent granted `FLASHBACK ANY TABLE` to `ow_billing_ro` (verified from the parent VM: `SELECT COUNT(*) FROM ow_billing.invoices AS OF SCN 2157784`). Recon source pins switch to `AS OF SCN`; the per-session read-only-transaction fallback is retired. | AS OF SCN pins | D10-8 | CLOSED | parent message "flashback granted" | STOP B | every recon source pin |

| DEC-C | 2026-09-14 | STOP C: P1 plan (`docs/migration/P1_monthly_invoicing_plan.md`, `stops/STOP_C.md`): 5 units / 3 single-batch waves, branch per batch, `AS OF SCN` pins, dependency decisions D2-1/2, D4-6, D5-1, D6-1/2, D7-2, D9-1/2/3, D10-5, D10-9/10/11 as PROPOSED there | approve as written with D7-2 option B / option A / amend | option B (like-for-like `sp_issue_invoice`; P2 emits invoice notifications) | APPROVED with amendment: identity fixed (SP), width 2 from wave 1 | parent relay of customer reply: "Approve STOP C: P1 plan as written, D7-2 option B. Fix identity: grant SP on ow_tp, width 2 from wave 1." | STOP C | P1 conversion scope, P2 notification scope |
| DEC-C1 | 2026-09-14 | Identity (FACT from parent): migration SP `dhrov_spa`, applicationId `2e90bc1d-e9a1-4703-8c48-ad28ebb1864d`, OAuth M2M via `DATABRICKS_CLIENT_ID`/`DATABRICKS_CLIENT_SECRET` + `DATABRICKS_DEMO_HOST`; ALL PRIVILEGES on `ow_tp`, READ on scope `ow_tp`, CAN_USE warehouse `565cd2fd713738c4`, Lakebase branch/credential rights on `ow-tp-billing`. PAT exception (DEC-A, D10-4) SUPERSEDED. Doctor re-run: `ready=true`. D10-11 closed: waves 1+ at width 2. U4 stays serial after U3 (wave 3) because it calls `rating.sp_finalize_rating`. Tier 6 stays `skipped/no_cdc` until parent says "cdc live"; Delta copies freeze-and-load until "sync enabled". | — | — | recorded | parent message | STOP C | every wave launch |
| DEC-C2 | 2026-09-14 | HALT before wave 0: D10-7b (guard hook not loaded in the orchestrator's own shell; workflow launch would need a false `blocked` probe result, refused). Options (a) platform fix, (b) direct-launch small-wave path. | (a) / (b) | (b) until (a) lands: children and verifier are separate sessions that run their own doctor + hook probe; orchestrator applies the workflow's collision and ledger gates by hand and records the deviation per wave | POSTED, waiting | — | D10-7b | wave launch |
