# 04 — Progress Ledger (one screen; doubles as cutover readiness view)

Run `tp-run/mongodb-20260901T205236Z` · target DB `ow_tp_mongodb_205236` (`Q` = `..._quarantine`) · ns `mongo_205236`

## Registered write targets (register BEFORE any load; collision = halt)

Status `PLANNED` = declared at phase 2, no load may start until STOP B and the unit flips it to `REGISTERED`.

| Collection (db.coll) | Unit | Registered (UTC) | Status |
|---|---|---|---|
| `codes`, `tenants`, `plans` | U0 | 2026-09-01 21:18 | REGISTERED |
| `customers`, `customers_history`, `counters`, Q.`dirty_signup_dt`, Q.`bad_csv_list` | U1 | 2026-09-01 23:01 | REGISTERED — `seq_billing_audit_log`/`seq_subscriptions_hist` seed rows added by U8-fix under orchestrator decision row 19 (F-X-1; `load_u1.py --counters-only`, source `USER_SEQUENCES.LAST_NUMBER`); independently verified 2026-09-02 06:47 UTC at #1457 @ `7791a93e`: exactly 5 counter docs, each `seq == LAST_NUMBER` (2/125000/1/11001/1), `$max` seed never rewinds (`advanced_preserved=[]`), golden collection sha unchanged by the fix pass (decision row 20) |
| `invoices`, Q.`invoice_feed_orphan_lines` | U2 | 2026-09-01 23:02 | REGISTERED |
| `documents`, `document_snapshots`, Q.`orphan_document_snapshots` | U3 | 2026-09-01 21:34 | REGISTERED |
| `files` | U4 | 2026-09-01 21:33 | REGISTERED |
| `subscriptions`, `subscriptions_history`, `usage_events`, `rating_periods`, `billing_invoices`, `credit_notes`, `dunning_attempts`, `notifications`, `billing_audit_log` | U5 | 2026-09-02 00:20 UTC | REGISTERED |
| `replay_u6_*` (clone of U5 set for Tier-4 replay) | U6 | 2026-09-02 01:06 UTC | REGISTERED |
| `replay_u7_*` (clone of U5 set + `plans`, `tenants` for Tier-4 rating replay) | U7 | 2026-09-02 01:04 UTC | REGISTERED |
| `replay_u8_*` (clone of U5 set + `plans`, `tenants` + `replay_u8_counters` for Tier-4 invoicing replay) | U8 | 2026-09-02 02:40 UTC | REGISTERED |
| `replay_u9_*` (clone of U5 `billing_invoices`, `subscriptions`, `dunning_attempts`, `notifications`, `billing_audit_log` + U0 `tenants` for Tier-4 dunning replay) | U9 | 2026-09-02 02:31 UTC | REGISTERED |

## Units

| Wave | Unit | Status | Parity (result.json) | Quarantine rate | Unverified paths | Cost | PR |
|---|---|---|---|---|---|---|---|
| 0 | U0 reference (`codes`,`tenants`,`plans`) | MERGED | GREEN — LIVE PASS (wave0 recon @ `892eb88a`, re-loaded from head: T1 3/3, T2 14/14, T3 104/104 full diff, 42/42 probes; `tp-run/mongodb-20260901T205236Z--wave0-recon:.migration/recon/wave_reports/wave0.md`) | 0% (no quarantine targets declared; expected 0, observed 0) | Tier 4 app-level parity (no recorded ops for U0; `PKG_OW_UTL.F_CODE_DESC` / `fn_list_plans` read paths untouched — hand-replayed in recon §3.19–3.20 only); harness stratified-sampling path (populations below threshold → full diff); derived read-time `plans.tier` DECODE (unpersisted; graded by consuming unit) | 2 recon runs, 4 loads (child) + 1 LIVE re-load/recon (parent) | #1423 |
| 1 | U1 customers (XL) | MERGED | GREEN — LIVE PASS (wave1 recon part1 @ `c5baa80a`, re-loaded from head: T1 3/3, T2 313/313, T3 33,333/33,333 (25,000 customers full diff + 8,333 graded embedded `attributes` + 0 history), 56/56 probes; `tp-run/mongodb-20260901T205236Z--wave1-recon-part1:.migration/recon/wave_reports/wave1.md`) | 0.324% (50 `dirty_signup_dt` + 31 `bad_csv_list` of 25,000; rows retained verbatim, twins null; sets 50/50, 31/31 in recon) | LIVE gate (now covered by wave1 recon part1); Tier 4 app write path (`customer_writes.py` — scratch-customer smoke only, `write_path_smoke.json`); mapping `derived_ungraded` twins (`signup_date`, arrays, `addresses`, `phones`) unit-tested, not recon-graded; load swap atomic per collection, not across the five; RPT-114 Oracle-vs-aggregation parity once + Flask test client, not under gunicorn; `counters` (D11) graded vs `USER_SEQUENCES` outside harness | 2 recon runs (run 1 red: full-mapping run touched non-U1 `DOCUMENTS`; run 2 on U1 projection green), 3 loads (child) + 1 LIVE re-load/recon (parent) | #1430 |
| 1 | U2 invoices | MERGED | GREEN — LIVE PASS (wave1 recon part1 pass 3 @ `9e73ffea`, re-loaded from head: T1 2/2, T2 9/9, T3 168,713/168,713 full diff (18,750 invoices + 149,963 graded embedded lines), 62/62 probes, 37/37 cross-unit; `tp-run/mongodb-20260901T205236Z--wave1-recon-part1-u2:.migration/recon/wave_reports/wave1.md`) | 0.197% (37/18,750 → Q.`invoice_feed_orphan_lines`; 0.025% of unit rows; set 37/37 in recon) | LIVE gate (now covered by wave1 recon part1 pass 3); Tier 4 (none in contract; RPT-114 month-end checked by `scripts/tp_mongo/rpt114_parity_u2.py`, exact match); harness stratified-sampling path (full_diff only); derived `invoice_date`/`due_date`/`lines[].gl_accounts` (derived_ungraded, loader unit tests only); admin-dashboard UI and `/api/reports/reconciliation` not exercised; loader drop+reinsert without staging swap (F-U2-2, runbook item) | 3 loads + 2 recons (merged-head re-run), ~5 min (child) + 1 LIVE re-load/recon (parent pass 3) | #1432 |
| 1 | U3 documents (Postgres) | MERGED | GREEN — LIVE PASS (wave1 recon: T1 3/3, T2 18/18, T3 16,260/16,260 incl. 13,876 embedded versions; `tp-run/mongodb-20260901T205236Z--wave1-recon:.migration/recon/wave_reports/wave1.md`) | 1.54% (6/390 snapshots → Q.`orphan_document_snapshots`; 0.037% of unit rows) | LIVE gate (now covered by wave1 recon); Tier 4 (none in contract); adapter `key_strata` (full_diff path only); `state_b64` never decoded | 2 recon runs (run 1 red: adapter stat names) | #1420 |
| 1 | U4 files (DynamoDB) | MERGED | GREEN — LIVE PASS (wave1 recon pass 2 @ `3420f475`: T1 1/1, T2 12/12, T3 10,000/10,000 full diff; `tp-run/mongodb-20260901T205236Z--wave1-recon:.migration/recon/wave_reports/wave1.md`) | 0% (no quarantine; 40 `orphaned_metadata` markers retained, not quarantined) | `orphaned_metadata` via S3 `HeadObject` (HEAD path untested; storage-key convention used); file-service (Rust) read/write path against `files` not exercised (data-layer parity only); `folder_id` missing-attribute branch (`null_missing_equiv`); only partition `ns='demo'` in fixture table | 7 loads + 4 recons, ~6 min (child) + 1 LIVE re-load/recon (parent pass 2) | #1419 |
| 2 | U5 billing core | MERGED | GREEN — LIVE PASS (wave2a recon @ `1aefd226`, re-loaded from head: T1 11/11, T2 53/53, T3 902/902 full diff incl. 3 embedded `results` + 2 embedded `lines`, 142/142 probes; run 1 on the pre-existing load FAIL triaged as observer-caused source drift in `BILLING_AUDIT_LOG` (0→1 row), resolved by the reload; `tp-run/mongodb-20260901T205236Z--wave2a-recon:.migration/recon/wave_reports/wave2a.md`) | 0% (no quarantine targets declared; expected 0, observed 0) | LIVE recon gate (now covered by wave2a recon); Tier 4 parity: PL/SQL not rewritten (D10, U6–U9; `ow_billing/__init__.py` is client factory + names only); `TRG_USAGE_EVENTS_CHECK` kind_cd-in-CODES branch: cross-collection, not `$jsonSchema`-expressible → U7 write path; `counters` seeds for `SEQ_SUBSCRIPTIONS_HIST`/`SEQ_BILLING_AUDIT_LOG` not written (`counters` is U1's target; note `SEQ_BILLING_AUDIT_LOG` now at 2 on source — seed from live sequence); TTL expiry unobserved (index option verified); stratified sampling path unexercised (full diff); load swap atomic per collection, not across the nine | 4 loads + 2 recon runs (child) + 1 LIVE re-load/recon (parent) | #1438 |
| 2 | U6 PKG_OW_UTIL+PKG_PLANS (calibration) | MERGED | GREEN — LIVE PASS (wave2b recon @ `f463577b`, re-loaded from head after preflight refused the child's replay residue: T1 14/14, T2 67/67, T3 1006/1006 full diff incl. 3 embedded `results` + 2 embedded `lines`, T4 5/5 PLANS-001…005, 71/72 probes — the 1 flag is the declared `f_str2dt` leniency gap F-U6-1; `tp-run/mongodb-20260901T205236Z--wave2b-recon:.migration/recon/wave_reports/wave2b.md`) | 0% (no quarantine targets declared; expected 0 new, observed 0 new; golden + 4 quarantine classes byte-identical to pre-fingerprint) | LIVE recon gate (now covered by wave2b recon); no deployment path supplies `MONGODB_ATLAS_URI` to legacy-billing (`_store()` lazy; 3 new endpoints error until configured — cutover wiring is orchestrator scope); golden (unprefixed) collections never written by U6 — app routes exercised only against `replay_u6_*` (`OW_BILLING_COLLECTION_PREFIX`); golden `counters` seeds for `seq_billing_audit_log`/`seq_subscriptions_hist` remain U1's; Flask blueprint HTTP layer unit-tested with fakes only, Tier-4 ran through `call_entrypoint` not HTTP; `f_str2dt` strict `DD-MON-YY` only (Oracle `TO_DATE` leniency unexercised); concurrent plan change (findOneAndUpdate conflict / txn retry) and `f_code_desc` (no PKG_PLANS caller) unit-tested only; F-X-1: U6/U7 `log_msg` ports carry incompatible `counters` contracts, neither matching golden — consolidate before U8/U9 | 4 loads + 2 fixture recon runs (child) + 1 LIVE re-load/recon (parent, 4 reloads) | #1440 |
| 2 | U7 PKG_RATING | MERGED | GREEN — LIVE PASS (wave2b recon @ `f05741f3`, re-loaded from head after the pre-existing clone failed T1 on the child's own RATING-008 finalize residue: T1 5/5, T2 23/23, T3 892/892 full diff incl. 3 embedded `results`, T4 8/8 RATING-001…008 (transcripts' `ORACLE_SOURCE_SHA` = live `USER_SOURCE`), 64/64 probes incl. 552 `fn_usage_rating` ops; `billing_audit_log` graded independently by the reviewer: equal 1/1 (F-U7-2); `tp-run/mongodb-20260901T205236Z--wave2b-recon:.migration/recon/wave_reports/wave2b.md`) | 0% (no quarantine targets declared; expected 0 new, observed 0 new; golden + 4 quarantine classes byte-identical to pre-fingerprint) | LIVE recon gate (now covered by wave2b recon); `billing_audit_log` excluded from U7 `GRADED_SOURCES` T1–T3 by the child (write-only sink; reviewer graded it 1/1 equal — optional re-inclusion is an orchestrator call); Tier-4 source side = recorded transcripts (`procs/oracle/transcripts/rating`, source SHA verified), not live PL/SQL; `/api/rating/*` routes not rewired (spec row 43, U6 calibration); suspension proration (status 20), NULL-plan propagation, `DUP_VAL_ON_INDEX` on `id` unit-tested only (no fixture rows); `TRG_USAGE_EVENTS_CHECK` kind_cd branch — rating never inserts usage; F-U7-1: re-finalising a fixture-seeded period whose id ≠ `md5(tenant||start)` succeeds on Mongo but raises `ORA-02291` on Oracle (behaviour decision pending); F-X-1 `counters` contract split with U6 | 3 loads + 2 recon runs (child) + 1 LIVE re-load/recon (parent, 5 reloads, 3 probe runs) | #1439 |
| 3 | U9 PKG_DUNNING | MERGED | GREEN — LIVE PASS (wave3 recon @ `9f67ec79`, pre-existing clone PASS then re-loaded from head and gated again, identical: T1 7/7, T2 39/39, T3 145/145 full diff, T4 5/5 DUNNING-001…005 (transcripts' `ORACLE_SOURCE_SHA` = live `USER_SOURCE`), 65/65 probes incl. `fn_overdue_accounts` 16 as_of, `sp_schedule_dunning` 7 runs, `sp_suspend_overdue` 5 runs + `/api/dunning/*` via test client; F-U9-1 informational (sorted tenant iteration vs unspecified Oracle `DISTINCT` order — audit row order only); `tp-run/mongodb-20260901T205236Z--wave3-recon:.migration/recon/wave_reports/wave3.md`) | 0% (no quarantine targets declared; expected 0 new, observed 0 new; golden + 4 quarantine classes byte-identical to pre-fingerprint) | LIVE recon gate (now covered by wave3 recon); `billing_audit_log` excluded from T1–T3 (write-only sink, as U7), its `counters` seed uses the U6 `util.log_msg` contract (`seq_billing_audit_log`/`seq`, F-X-1) on the clone only; Tier-4 source side = recorded transcripts `procs/oracle/transcripts/dunning` (source SHA verified), not live PL/SQL; `JOB_NIGHTLY_DUNNING` replacement (`ow_billing/jobs.py`) never scheduled/activated — only the env-gated CLI path unit-tested; Flask routes `/api/dunning/*` unit-tested with fakes, Tier 4 calls Python entrypoints directly, routes carry no auth (matches legacy); `subscriptions_history` append unit-tested only (golden 0 rows); `DuplicateKeyError` no-op, non-duplicate error propagation and notification insert race unit-tested only | 4 loads + 3 fixture recon runs (child, fix rounds 1–2) + 1 LIVE gate + re-load/re-gate (parent, 4 reloads) | #1444 |
| 3 | U8 PKG_INVOICING | MERGED | GREEN — fix pass MERGED (#1457 @ `7791a93e`, merged `5fe2af81`) on independent LIVE PASS (re-loaded from head: T1 8/8, T2 36/36, T3 902/902, T4 6/6 INVOICE-001…006, idempotent re-run identical; U5 T1–T3 / U6 T1–T4 / U7 T1–T4 regression PASS; 26/26 probes: every `billing_invoices.lines[]` carries `invoice_id == parent _id` on golden + all clones, `counters` 5/5 == `USER_SEQUENCES.LAST_NUMBER`, util/rating/invoicing `log_msg` ids monotonic non-colliding on scratch; F-U8-1 + F-X-1 CLOSED; `tp-run/mongodb-20260901T205236Z--fix-recon:.migration/recon/wave_reports/fix_pass.md`); prior: GREEN — LIVE PASS (wave3 recon @ `0024b45e`, re-loaded from head after the pre-existing clone failed T1 on the child's own `sp_issue_invoice` replay residue (6/6/17 vs source 3/3/2, Oracle re-counted twice, stable — not source drift): T1 8/8, T2 36/36, T3 902/902 full diff incl. 3 embedded `results` + 2 embedded `lines`, T4 6/6 INVOICE-001…006 (transcripts' `ORACLE_SOURCE_SHA` = live `USER_SOURCE`), 76/85 probes — the 9 flags are F-U8-1 (rebuilt `lines[]` omit the mapped `invoice_id` field, ×8; one-line fix recommended before cutover) and F-U8-2 (inherited U7 F-U7-1: issuing tenant 1's fixture-seeded Jan-2026 period succeeds on Mongo where Oracle raises `ORA-02291`, ×1); 280 `fn_invoice_preview` ops, 10 `sp_issue_invoice` paths, 4 `fn_invoice_lines` calls all equal; `tp-run/mongodb-20260901T205236Z--wave3-recon:.migration/recon/wave_reports/wave3.md`) | 0% (no quarantine targets declared; expected 0 new, observed 0 new; golden + 4 quarantine classes byte-identical to pre-fingerprint) | LIVE recon gate (now covered by wave3 recon); Tier-4 source = recorded Oracle transcripts (source SHA verified), not live PL/SQL; `sp_finalize_rating` (U7 module, no session API) runs before the invoice/credit transaction; invoice replaceOne + credit-note burn-down are one multi-document transaction with conditional (`_id`+`remaining_amount`) updates; a failure after finalize but before/inside the txn leaves the rating period finalized (Oracle would roll it back); concurrency guarded by conditional updates + in-txn recompute, exercised by unit tests only; NULL plan / exempt / no-credit branches unit-tested only; routes not wired (no HTTP route for invoicing; golden `/api/invoices/*` still targets Postgres `billing.*`); `billing_audit_log` not graded T1–T3 (shared `INVOICING` rows = 0 checked); sibling `replay_u9_subscriptions_history` (empty) appeared during the child run, untouched; F-U8-1 fixed (u8-fix); F-X-1 fixed: all `log_msg` sites → `util.log_msg` (`seq_billing_audit_log`/`seq`), loaders seed from `USER_SEQUENCES`; U7 `rating.log_msg` unseeded-reconcile behaviour removed (unseeded counter now raises `LookupError`) — two U7 unit tests rewritten to the consolidated contract; U5 regression gate T1 `billing_audit_log` 0 (fresh fixture) vs 1 (golden, pre-existing drift row) — not owned by U8-fix; harness preflight needs `.migration/allowed_targets.json` (absent on branch, created locally, uncommitted) | 4 loads + 2 fixture recon runs (child) + 1 LIVE gate (FAIL, residue) + re-load/re-gate (parent, 4 reloads, 3 probe runs); fix round: 2 loads + 2 fixture recon runs + U5/U7 regression gates (child) + independent LIVE fix-pass recon (parent: 5 loads, U8 gate ×2, U5/U6/U7 gates, 26 probes) | #1447, #1457 |

Status values: NOT_STARTED · CONTRACT · LOADING · RECON_RED · RECON_GREEN · IN_REVIEW · MERGED · HALTED
