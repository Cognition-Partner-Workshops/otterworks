# Census coverage — every OW_BILLING object in exactly one bucket

Generated by `.migration/tools/build_coverage.py` from the read-only census in
`.migration/census/`. Buckets: **unit** (a migration unit owns it), **shared**
(wave-0 reference data), **rewrite** (stored logic that becomes application or
pipeline code), **retire** (has no target-side equivalent, by decision),
**proposed-unused** (present but empty), **excluded** (out of scope).

## Tables (20)

| Table | Rows | Bucket | Owning unit | Target collection | Wave |
|---|---:|---|---|---|---|
| `INVOICE_LINE` | 150,000 | unit | `invoices` | `invoices` | 2 |
| `CUSTOMER_MASTER` | 25,000 | unit | `customers` | `customers` | 1 |
| `INVOICE_HEADER` | 18,750 | unit | `invoices` | `invoices` | 2 |
| `ENTITY_ATTR_VALUE` | 8,333 | unit | `customers` | `customers` | 1 |
| `USAGE_EVENTS` | 814 | unit | `usage_rating` | `usage_events` | 2 |
| `SUBSCRIPTIONS` | 69 | unit | `subscriptions` | `subscriptions` | 1 |
| `TENANTS` | 69 | shared | `reference` | `tenants` | 0 |
| `CODES` | 32 | shared | `reference` | `codes` | 0 |
| `CREDIT_NOTES` | 5 | unit | `collections_ops` | `credit_notes` | 3 |
| `INVOICES` | 3 | unit | `subscription_invoices` | `subscription_invoices` | 2 |
| `PLANS` | 3 | shared | `reference` | `plans` | 0 |
| `RATING_PERIODS` | 3 | unit | `usage_rating` | `rating_periods` | 2 |
| `RATING_RESULTS` | 3 | unit | `usage_rating` | `rating_periods` | 2 |
| `INVOICE_LINES` | 2 | unit | `subscription_invoices` | `subscription_invoices` | 2 |
| `DUNNING_ATTEMPTS` | 1 | unit | `collections_ops` | `dunning_attempts` | 3 |
| `FIXTURE_META` | 1 | excluded | — | — | — |
| `NOTIFICATIONS` | 1 | unit | `collections_ops` | `notifications` | 3 |
| `BILLING_AUDIT_LOG` | 0 | unit | `collections_ops` | `billing_audit_log` | 3 |
| `CUSTOMER_MASTER_HIST` | 0 | unit | `customers` | `customers` | 1 |
| `SUBSCRIPTIONS_HIST` | 0 | unit | `subscriptions` | `subscriptions` | 1 |

Arithmetic: 20 tables = 19 bucketed into units/shared + 1 excluded. Total source rows 203,089; total target documents 44,750 (the difference is child rows that become embedded array elements).

## Columns (432)

Every column lands in `fields` (mapped, including the repeating groups folded
into subdocuments and the child tables folded into arrays) or `dropped` (with
a reason) of exactly one collection — `build_mapping_spec.py` fails the build
otherwise.

| Collection | Mapped | Dropped |
|---|---:|---:|
| `codes` | 3 | 0 |
| `tenants` | 4 | 0 |
| `plans` | 7 | 0 |
| `customers` | 87 | 233 |
| `subscriptions` | 16 | 1 |
| `invoices` | 22 | 7 |
| `usage_events` | 5 | 0 |
| `rating_periods` | 12 | 1 |
| `subscription_invoices` | 13 | 1 |
| `credit_notes` | 5 | 0 |
| `dunning_attempts` | 6 | 0 |
| `notifications` | 4 | 0 |
| `billing_audit_log` | 4 | 0 |
| _excluded tables_ | 0 | 1 |

Arithmetic: 188 mapped + 243 dropped + 1 in excluded tables = 432 of 432 source columns.

**proposed-unused: 113 of `CUSTOMER_MASTER`'s 155 columns are NULL in all 25,000 rows** — the whole mailing-address block, `PHONE3`/`PHONE4`, `EMAIL_2`/`EMAIL_3`, `FLAG_01..20`, `UDF_01..40`, `UDF_AMT_01..10`, `UDF_DT_01..10` and 12 others. They are dropped with the population count as evidence (`census/access_patterns.json#customer_master_population`), not carried forward as empty fields. `CUSTOMER_MASTER_HIST` mirrors the same 155 columns plus 3 history columns and holds 0 rows.

## Stored logic (5 packages / 19 routines, 7 triggers, 2 jobs, 5 sequences)

All of it is owned by the `stored_logic` unit (wave 4). No PL/SQL is ported
as-is; each object gets a destination.

| Object | Type | Bucket | Destination |
|---|---|---|---|
| `PKG_OW_UTIL` | package | rewrite | shared helpers: f_md5_uuid -> driver-side UUIDv5, f_code_desc -> the denormalized code label, f_dt2str / f_str2dt -> the load-time date parse, log_msg -> application logging |
| `PKG_PLANS` | package | rewrite | fn_list_plans / fn_entitlement -> aggregation pipelines (as-of filter on subscriptions); sp_change_plan -> a transactional service method (multi-document txn) |
| `PKG_RATING` | package | rewrite | compute_rating / fn_usage_rating / fn_usage_summary -> one $group aggregation over usage_events; sp_finalize_rating -> a bulkWrite upsert into rating_periods.results[] |
| `PKG_INVOICING` | package | rewrite | compute_preview / fn_invoice_preview / fn_invoice_lines -> aggregation over rating_periods; sp_issue_invoice -> a single insert of the embedded invoice document |
| `PKG_DUNNING` | package | rewrite | fn_overdue_accounts -> aggregation; sp_schedule_dunning / sp_suspend_overdue -> the scheduled job that replaces JOB_NIGHTLY_DUNNING |
| `TRG_CUSTOMER_MASTER_SEQ` | trigger | retire | sequence-backed surrogate; cust_seq_no has no external consumer, `_id` is the natural key |
| `TRG_ENTITY_ATTR_VALUE_SEQ` | trigger | retire | EAV rows become array elements; the surrogate disappears with the table |
| `TRG_BILLING_AUDIT_LOG_ID` | trigger | retire | replaced by ObjectId on the audit collection |
| `TRG_CUSTOMER_MASTER_HIST` | trigger | rewrite | full-row version history -> append to the embedded history[] array in the same update |
| `TRG_SUBSCRIPTIONS_HIST` | trigger | rewrite | same pattern on subscriptions.history[] |
| `TRG_SUB_NO_UNCANCEL` | trigger | rewrite | genuine data-integrity invariant (a cancelled subscription cannot be reactivated): enforced by a JSON-schema validator plus a guarded update filter |
| `TRG_USAGE_EVENTS_CHECK` | trigger | rewrite | insert-time validation -> $jsonSchema validator on usage_events |
| `JOB_NIGHTLY_DUNNING` | job | rewrite | scheduled application job calling the dunning service; not an Atlas trigger (it is batch work) |
| `JOB_PURGE_AUDIT_LOG` | job | rewrite | replaced by a TTL index on billing_audit_log.logged_at |
| `SEQ_CUSTOMER_MASTER` | sequence | retire | no external consumer of cust_seq_no |
| `SEQ_CUSTOMER_MASTER_HIST` | sequence | retire | history elements are keyed by position + hist_dt |
| `SEQ_SUBSCRIPTIONS_HIST` | sequence | retire | same |
| `SEQ_ENTITY_ATTR_VALUE` | sequence | retire | EAV rows become array elements |
| `SEQ_BILLING_AUDIT_LOG` | sequence | retire | ObjectId |

Compilation status: 17 PL/SQL objects, 0 invalid. ROWID usage in stored code: 0 objects (the profile's ROWID trap does not apply to this estate).

## Indexes (25)

Source indexes are not migrated mechanically; the target index plan is per
collection in `03_mapping_spec.json#collections.*.indexes` and is derived from
the access-pattern evidence, not from the Oracle index list. Every unique
constraint in the source does survive: as the collection key, a unique index,
or a declared element invariant inside an embedded array, asserted by
`build_mapping_spec.py`.

## Behavioural baseline

`procs/transcripts/` holds 24 recorded golden transcripts across the four
business modules (rating 8, invoicing 6, plans 5, dunning 5) with a pinned
`SOURCE_SHA`/`FIXTURE_SHA`. They are the behavioural contract for the
`stored_logic` unit: the rewritten logic must reproduce them, and they are
independent of the data-parity recon.
