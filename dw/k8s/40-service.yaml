apiVersion: v1
kind: Service
metadata:
  name: analytics-dw
  namespace: legacy-dw
  labels:
    app.kubernetes.io/name: analytics-dw
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analytics-dw
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
