apiVersion: v1
kind: Service
metadata:
  name: analytics-dw-host
  namespace: legacy-dw
  labels:
    app.kubernetes.io/name: analytics-dw
    app.kubernetes.io/component: host-access
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: analytics-dw
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      nodePort: 30032
      protocol: TCP
