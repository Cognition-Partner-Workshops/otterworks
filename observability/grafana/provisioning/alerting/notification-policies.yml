apiVersion: 1

policies:
  - orgId: 1
    receiver: Default
    group_by: ['alertname', 'affected_service']
    group_wait: 10s
    group_interval: 30s
    repeat_interval: 4h
    routes:
      - receiver: Default
        object_matchers:
          - ['team', '=', 'otterworks']
