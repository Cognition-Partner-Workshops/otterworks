[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    spark: requires a local PySpark session (pyspark + a JDK on the box)
filterwarnings =
    ignore::DeprecationWarning
