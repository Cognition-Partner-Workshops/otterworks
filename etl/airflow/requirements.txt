# OtterWorks ETL Pipeline Dependencies
# Pin to compatible versions for reproducibility

# Airflow core
apache-airflow==2.10.4
apache-airflow-providers-amazon==9.2.0
apache-airflow-providers-postgres==5.13.1

# AWS SDK
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# Data processing
pyarrow>=15.0.0,<16.0.0

# HTTP client (for search reindex)
requests>=2.31.0,<3.0.0

# Testing
pytest>=8.0.0,<9.0.0
pytest-cov>=4.1.0,<5.0.0
moto[dynamodb,s3,sqs]>=5.0.0,<6.0.0
